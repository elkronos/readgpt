% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{gr_reader_signature}
\alias{gr_reader_signature}
\title{The traversal signature of a reader}
\usage{
gr_reader_signature(reader)
}
\arguments{
\item{reader}{Reader name, or a \code{gr_read_spec}.}
}
\value{
A single string \code{"select|calls|state"}, e.g. \code{"topk|1|none"}. For
\code{ensemble} the member list is appended in braces
(\code{"ensemble|sum+1|none{map_reduce+retrieve}"}), so two ensembles with
different members are correctly seen as different experiments.
}
\description{
The signature is how the package tells two reading \emph{methodologies} apart from
two names for the same thing. It encodes which chunks reach the model
(\code{select}), the call pattern (\code{calls}), and whether information flows between
calls (\code{state}).
}
\examples{
# Same document, different traversal: one call over everything, versus one
# call per chunk plus merges.
gr_reader_signature("stuff")
gr_reader_signature("map_reduce")

# An ensemble's signature carries its members, so two differently-composed
# ensembles are not mistaken for each other.
gr_reader_signature(gr_read_spec("ensemble", members = c("retrieve", "refine")))
}
\seealso{
\code{\link[=gr_readers]{gr_readers()}} for every signature at once, \code{\link[=gr_compare]{gr_compare()}} which
uses this to detect duplicate recipes, \code{\link[=gr_register_reader]{gr_register_reader()}}

Other reading functions: 
\code{\link{gr_answer}},
\code{\link{gr_read}()},
\code{\link{gr_read_spec}()},
\code{\link{gr_readers}()},
\code{\link{gr_register_reader}()},
\code{\link{is_not_found}()},
\code{\link{new_answer}()}
}
\concept{reading functions}
