% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-state.R
\name{gr_options}
\alias{gr_options}
\title{Get or set package options}
\usage{
gr_options(...)
}
\arguments{
\item{...}{Nothing, a single option name, \code{name = value} pairs, or a named
list (the form \code{gr_options()} itself returns, so a saved value can be
restored directly).}
}
\value{
The option list, a single option value, or (when setting) the old
values invisibly.
}
\description{
\code{gr_options()} with no arguments returns the full option list. Called with
a single string it returns that option. Called with \code{name = value} pairs it
sets them and invisibly returns the previous values, so it composes with
\code{on.exit()}.
}
\details{
Options are read at \emph{call} time, never captured at load time, so changing an
option mid-session affects subsequent runs.
}
\section{Options}{

\describe{
\item{\code{verbose} (TRUE)}{Print progress for each ingest/segment/read stage.}
\item{\code{model} ("gpt-5.6-terra")}{Default chat model. Note the default is a
reasoning model, which does not accept \code{temperature}.}
\item{\code{embedding_model} ("text-embedding-3-small")}{Default embedding model.}
\item{\code{tokenizer} ("heuristic")}{Token counter: \code{"heuristic"} (conservative,
no dependencies), \code{"words"}, \code{"chars"}, \code{"tiktoken"} (needs reticulate),
or a name registered via \code{\link[=gr_set_tokenizer]{gr_set_tokenizer()}}.}
\item{\code{api} ("responses")}{\code{"responses"} or \code{"chat"} request shape.}
\item{\code{api_base} ("https://api.openai.com/v1")}{API root; point this at a
proxy or a compatible endpoint.}
\item{\code{temperature} (NULL)}{Default sampling temperature. \code{NULL} omits the
field. Dropped automatically for models that reject it.}
\item{\code{max_retries} (4)}{Retries for transient failures. HTTP 400 is never
retried -- a malformed request stays malformed.}
\item{\code{retry_pause_base} (2)}{Seconds; exponential backoff base.}
\item{\code{request_timeout} (120)}{Per-request timeout, seconds.}
\item{\code{safety_margin} (0.10)}{Fraction of the context window left unused to
absorb tokenizer error. Not a spending cap -- see \code{max_cost_usd}.}
\item{\code{min_output_tokens} (256)}{Floor on the completion room \code{\link[=gr_budget]{gr_budget()}}
reserves \emph{when \code{reserve_output} is not given explicitly}. An explicit
\code{reserve_output} is honoured down to 1.}
\item{\code{cache_documents} (TRUE)}{Cache ingestion per file + settings. The key
covers file size, mtime and every option that changes the output.}
\item{\code{cache_embeddings} (TRUE)}{Cache embeddings per text + model.}
\item{\code{embedder} (NULL)}{Which registered embedder to use. \code{NULL} means the
one the client carries, if any, and otherwise \code{"api"}. Naming one here
overrides both. See \code{\link[=gr_embedders]{gr_embedders()}}; recording a run with a
\emph{deterministic} embedder is what lets a \code{\link[=gr_replay_client]{gr_replay_client()}} reproduce its
chunk ranking.}
\item{\code{cache_dir} (NULL)}{Directory \code{\link[=gr_cache]{gr_cache()}} stores model responses in.
\code{NULL} means a per-session directory under \code{tempdir()}, which costs
nothing and disappears with the session. Set it to a real path, such as
\code{tools::R_user_dir("readgpt", "cache")}, to keep responses across
sessions and make a long run resumable.}
\item{\code{parallel} (FALSE)}{Run per-chunk calls concurrently. Needs the
future and future.apply packages; without them it warns and runs
sequentially.}
\item{\code{workers} (4)}{Worker processes when \code{parallel} is TRUE.}
\item{\code{max_cost_usd} (5)}{Refuse a run whose pre-flight estimate exceeds
this, in USD. \code{NULL} disables the check.}
\item{\code{max_calls} (400)}{Hard cap on model calls per run, checked before
the first call and again before every subsequent one. \code{NULL} removes the
cap.}
\item{\code{unknown_model_action} ("warn")}{\code{"warn"} or \code{"error"} when a model id
is not in the registry.}
}
}

\examples{
old <- gr_options(verbose = FALSE)
gr_options("verbose")
gr_options(old)
}
\seealso{
\code{\link[=gr_register_model]{gr_register_model()}} to correct a model's limits,
\code{\link[=gr_set_tokenizer]{gr_set_tokenizer()}}, \code{\link[=gr_budget]{gr_budget()}}, \code{\link[=gr_cache]{gr_cache()}}
}
