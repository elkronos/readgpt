% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readgpt-package.R
\name{gr_result}
\alias{gr_result}
\title{The result of one model call}
\description{
Returned by \code{\link[=gr_call]{gr_call()}}. Designed so a caller can never be handed \code{NULL} or
\code{character(0)} where a string is expected.
}
\section{Fields}{

\describe{
\item{\code{ok}}{Logical(1). \code{FALSE} for transport failures, HTTP errors, and
\strong{successful calls that returned no text} (a refusal or content filter).}
\item{\code{text}}{Character(1), always. \code{""} when \code{ok} is \code{FALSE}. Never \code{NULL},
never \code{character(0)}, and newlines are preserved.}
\item{\code{error}}{Character or \code{NULL}. What went wrong, including the HTTP body
for API-side errors.}
\item{\code{usage}}{List with \code{input} and \code{output} token counts as reported by
the API.}
\item{\code{status}, \code{finish_reason}, \code{retryable}, \code{model}, \code{raw}}{HTTP status,
the API's stop reason, whether a retry could help, the model id, and the
parsed response body.}
}
}

\section{Methods}{

\code{print()} shows \code{ok}, the model, the token usage and either the text or the
error.
}

\examples{
res <- gr_call(gr_mock_client(function(m, p) "hello"), "hi")
c(ok = res$ok, text = res$text)
}
\seealso{
\code{\link[=gr_call]{gr_call()}} which returns one, \code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_mock_client]{gr_mock_client()}}

\code{\link[=gr_call]{gr_call()}}, \code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_mock_client]{gr_mock_client()}}
}
