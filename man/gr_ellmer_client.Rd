% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-backend.R
\name{gr_ellmer_client}
\alias{gr_ellmer_client}
\title{Read documents through an ellmer chat}
\usage{
gr_ellmer_client(chat, embed = NULL, model = NULL)
}
\arguments{
\item{chat}{An ellmer \code{Chat}, e.g. from \code{ellmer::chat_anthropic()} or
\code{ellmer::chat_ollama()}.}

\item{embed}{Optional function of \verb{(texts, params)} returning one row per
input -- for example a thin wrapper around \code{ragnar::embed_ollama()}. Without
it, \code{retrieve} and the \code{semantic} segmenter fall back to lexical vectors and
warn.}

\item{model}{Model id reported to this package. Defaults to the chat's own
model. Register it with \code{\link[=gr_register_model]{gr_register_model()}} if it is not already known --
the context window is what sizes your chunks, so a wrong one is not cosmetic.}
}
\value{
A \code{\link[=gr_backend_client]{gr_backend_client()}}.
}
\description{
Uses an \code{ellmer} \code{Chat} object as the transport, so every provider ellmer
supports -- Anthropic, Google, Bedrock, Azure, Ollama, Hugging Face, and the
rest -- becomes available to every reading strategy here, with this package's
context budgeting, cost rails, traces, caching and replay unchanged around it.
}
\details{
ellmer is a suggested dependency: this function is the only thing in the
package that needs it.
}
\section{Requirements on the chat}{

The adapter calls \verb{$chat()}, \verb{$clone()}, \verb{$set_turns()} and
\verb{$set_system_prompt()}, and refuses a chat missing any of them
(\code{gr_bad_backend}). The last two are not conveniences: this package puts its
instructions in the system prompt, and it clears turns so that one chunk's
call cannot leak into the next. A chat that silently dropped either would
produce unconstrained answers with nothing to show for it.
}

\section{What does not carry over}{

Two things, both worth knowing before you rely on them.

\code{temperature} belongs to the chat object, not to the call. ellmer fixes
sampling parameters when the chat is constructed, so a \code{temperature} in a
\code{\link[=gr_read_spec]{gr_read_spec()}} cannot be honoured per-call; it is ignored and warned about
once (\code{gr_ellmer_temperature}). Build a second chat if you need a second
temperature.

Each call is independent. An ellmer chat accumulates turns, and this package
issues many unrelated calls per run, so every call runs against a fresh deep
clone with its turns cleared. Your chat object is never mutated, and no
conversation history leaks from one chunk's call into the next.
}

\examples{
\dontrun{
library(ellmer)

# Any provider ellmer speaks to, with any reading strategy here.
cl <- gr_ellmer_client(chat_anthropic(model = "claude-sonnet-4-5"))
ans <- answer_document("report.pdf", "What was revenue?", "thorough", client = cl)

# Locally, for nothing:
local <- gr_ellmer_client(chat_ollama(model = "llama3.1"))
gr_compare("report.pdf", "What was revenue?",
           c("fast", "thorough"), client = local)$summary
}
}
\seealso{
\code{\link[=gr_backend_client]{gr_backend_client()}}, \code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_register_model]{gr_register_model()}}
}
