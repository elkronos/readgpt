% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/readgpt-package.R
\name{gr_answer}
\alias{gr_answer}
\title{The result of one reading run}
\description{
Returned by \code{\link[=gr_read]{gr_read()}} and, with three extra fields, by \code{\link[=answer_document]{answer_document()}}.
}
\section{Fields}{

\describe{
\item{\code{answer}}{Character(1). Always a single string. The sentinel
\code{"NOT_IN_DOCUMENT"} means the model reported the document does not
contain the answer -- test it with \code{is_not_found()}-style matching rather
than substring search.}
\item{\code{partial}}{Logical(1). \code{TRUE} when anything degraded: a call failed,
chunks were dropped, a cap was hit, a strategy fell back. \strong{Check this
before trusting an answer.}}
\item{\code{notes}}{List. Why it is partial, and per-reader detail:
\code{dropped_chunks}, \code{failed_calls}, \code{error}, \code{merge_levels},
\code{degraded_to_bm25}, \code{stop_reason}, \code{call_cap_reached}, and so on. Two are
set for every reader: \code{cited_unknown}, chunk ids the answer cited that
were never sent, and \code{unverified_evidence}, the number of quoted spans
that are not in the chunk they claim to come from. Either makes the answer
\code{partial}.}
\item{\code{evidence}}{Data frame or \code{NULL}, with columns \code{chunk_id}, \code{text},
\code{page}, \code{section}, \code{score}. \strong{What \code{text} holds depends on the reader}:
verbatim chunk text for \code{stuff}, \code{retrieve}, \code{rerank} and \code{iterative};
model-extracted passages for \code{skim}; per-chunk model answers for
\code{map_reduce}. \code{refine} and \code{hierarchical} return \code{NULL}. \code{page} is
populated only for PDF sources; \code{score} only for \code{retrieve} (cosine) and
\code{rerank} (0-10, model-judged). Where the evidence is \emph{model-written} --
\code{skim} -- three more columns appear: \code{source_text}, the chunk the span
claims to quote, plus \code{verified} and \code{match} from checking one against
the other; see \code{\link[=gr_verify_evidence]{gr_verify_evidence()}}. Readers whose evidence is verbatim
chunk text do not carry them, because the span and its source are the
same string. A blank roxygen line inside a \verb{\\describe\{\}} item ends the
item, which is why this is one paragraph.}
\item{\code{chunks_used}}{Integer vector of \code{chunk_id}s that CONTRIBUTED to the answer. For the
per-chunk readers this is a subset of the chunks actually sent -- a chunk
that answered \code{NOT_IN_DOCUMENT} was read and paid for but is not listed.
\code{notes$chunks} reports how many were sent.}
\item{\code{reader}, \code{signature}}{Which strategy ran, and its traversal
signature (see \code{\link[=gr_reader_signature]{gr_reader_signature()}}).}
\item{\code{question}}{The question, as asked.}
\item{\code{trace}}{The \link{gr_trace} for this run. In a \code{\link[=gr_compare]{gr_compare()}} the trace is
shared across recipes, so it records every recipe's calls.}
\item{\code{recipe}, \code{document}, \code{segmentation}}{Added by \code{\link[=answer_document]{answer_document()}} and
\code{\link[=gr_compare]{gr_compare()}}: the recipe name, the source and ingestion stats, and the
chunk statistics the reader saw.}
}
}

\section{Methods}{

\code{print()} shows the answer, the partial flag, the call count and an evidence
summary; \code{\link[=as_json]{as_json()}} serialises the answer together with every prompt and
response from the same run.
}

\examples{
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")
ans <- answer_document(readgpt_example(), "What was revenue?", "fast", client = cl)
ans$partial
ans$evidence[, c("chunk_id", "page", "score")]
}
\seealso{
\code{\link[=answer_document]{answer_document()}} and \code{\link[=gr_read]{gr_read()}} which return one, \code{\link[=gr_compare]{gr_compare()}}
to compare several, \code{\link[=is_not_found]{is_not_found()}} to test the sentinel, \code{\link[=as_json]{as_json()}},
\code{\link[=new_answer]{new_answer()}} to build one in a custom reader

\code{\link[=gr_read]{gr_read()}}, \code{\link[=answer_document]{answer_document()}}, \code{\link[=as_json]{as_json()}}

Other reading functions: 
\code{\link{gr_read}()},
\code{\link{gr_read_spec}()},
\code{\link{gr_reader_signature}()},
\code{\link{gr_readers}()},
\code{\link{gr_register_reader}()},
\code{\link{is_not_found}()},
\code{\link{new_answer}()}
}
\concept{reading functions}
