% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-clean.R
\name{gr_clean}
\alias{gr_clean}
\title{Run the cleaning pipeline over a character vector}
\usage{
gr_clean(text, steps = NULL, opts = list())
}
\arguments{
\item{text}{Character vector of block texts.}

\item{steps}{Character vector of cleaner \strong{names} (see \code{\link[=gr_cleaners]{gr_cleaners()}}), or
\code{NULL} for the \code{default_on} set. Unlike \code{\link[=gr_ingest_spec]{gr_ingest_spec()}}'s \code{clean}
argument this does \strong{not} accept preset names. Steps are reordered so every
\code{"early"} cleaner runs before every \code{"late"} one, regardless of the order
given -- this is what stops digit removal from running before the page and
figure filters that need digits to match.}

\item{opts}{Named list passed to every step.}
}
\value{
The cleaned character vector, with a \code{"gr_clean_log"} attribute
recording characters removed per step.
}
\description{
Exposed separately from \code{\link[=gr_ingest]{gr_ingest()}} so you can see exactly what a cleaning
configuration does to your own text before committing a run to it. Cleaning
is destructive and irreversible from the model's point of view: whatever is
removed here, no reading strategy can recover.
}
\examples{
# Listed late-then-early, but page_numbers still runs FIRST -- otherwise
# remove_numbers eats the "1" and "Page" survives as body text.
out <- gr_clean(c("Page 1", "Revenue rose to 45.2 million in 2024."),
                steps = c("remove_numbers", "page_numbers"))
out[]

# Every step reports what it took out.
vapply(attr(out, "gr_clean_log"), function(s) s$chars_removed, integer(1))
}
\seealso{
\code{\link[=gr_cleaners]{gr_cleaners()}} for the step names, \code{\link[=gr_register_cleaner]{gr_register_cleaner()}},
\code{\link[=gr_ingest_spec]{gr_ingest_spec()}} to use a configuration in a real run

Other ingest functions: 
\code{\link{gr_cleaners}()},
\code{\link{gr_document}},
\code{\link{gr_extractors}()},
\code{\link{gr_ingest}()},
\code{\link{gr_ingest_spec}()},
\code{\link{gr_register_cleaner}()},
\code{\link{gr_register_extractor}()}
}
\concept{ingest functions}
