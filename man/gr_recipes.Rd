% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{gr_recipes}
\alias{gr_recipes}
\title{Ready-made recipes}
\usage{
gr_recipes(name = NULL)
}
\arguments{
\item{name}{Optional recipe name, or a character vector of names; omit to
list them all. A single name returns a \code{gr_recipe}; several return a named
list of them.}
}
\value{
A \code{gr_recipe} when \code{name} is a single string, otherwise a named list
of \code{gr_recipe}s.
}
\description{
Named starting points that pair a segmentation strategy with a reader that
suits it. Each is a plain \code{gr_recipe}, so you can modify any field.
}
\section{\code{"auto"}}{

\code{\link[=answer_document]{answer_document()}} defaults to \code{recipe = "auto"}. It is not a recipe but a
choice between two of these, made once the document is ingested: \code{"fast"}
for a document of at most 50,000 tokens (less on a model with a small
context window), \code{"thorough"} for anything longer. "Choosing the recipe" in
\code{\link[=answer_document]{answer_document()}} gives the whole rule. Functions that read several
documents or compare recipes take one fixed recipe, and refuse \code{"auto"}.
}

\examples{
# What each built-in actually is, in one table.
do.call(rbind, lapply(names(gr_recipes()), function(n) {
  r <- gr_recipes(n)
  data.frame(recipe = n, clean = paste(as.character(r$ingest$clean), collapse = "+"),
             segment = r$segment$method, max_tokens = r$segment$max_tokens,
             reader = r$read$reader)
}))

gr_recipes("precise")
}
\seealso{
\code{\link[=gr_recipe]{gr_recipe()}} to build your own, \code{\link[=answer_document]{answer_document()}}, \code{\link[=gr_compare]{gr_compare()}},
\code{\link[=gr_segmenters]{gr_segmenters()}} and \code{\link[=gr_readers]{gr_readers()}} for the pieces they are made of
}
