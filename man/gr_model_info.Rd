% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-models.R
\name{gr_model_info}
\alias{gr_model_info}
\title{Look up a model's capabilities}
\usage{
gr_model_info(model = NULL)
}
\arguments{
\item{model}{Model id.}
}
\value{
A named list: \code{id}, \code{context_window}, \code{max_output}, \code{input_usd} and
\code{output_usd} (per 1M tokens, used by \code{\link[=gr_estimate_cost]{gr_estimate_cost()}}), \code{reasoning},
\code{supports_temperature}, \code{kind}, \code{dimensions}, \code{as_of}, \code{certain}, and
\code{source} (\code{"registered"}, \code{"builtin"}, \code{"alias"}, \code{"pattern:..."} or
\code{"default"}). \code{certain} is \code{FALSE} when the answer came from a family regex
or the fallback -- verify before relying on it for cost control.
}
\description{
Resolution order: user-registered exact id, built-in exact id, alias, family
regex, then a conservative default. The returned list always carries
\code{certain}, which is \code{FALSE} when the answer came from a regex or the default
-- treat that as "verify before trusting for cost control".
}
\examples{
gr_model_info("gpt-4o")[c("context_window", "max_output", "certain")]

# An unrecognised id warns and falls back to conservative limits.
suppressWarnings(gr_model_info("some-model-from-next-year")$certain)
}
\seealso{
\code{\link[=gr_register_model]{gr_register_model()}}, \code{\link[=gr_models]{gr_models()}}, \code{\link[=gr_budget]{gr_budget()}}

Other cost and token functions: 
\code{\link{gr_budget}()},
\code{\link{gr_count_tokens}()},
\code{\link{gr_estimate_cost}()},
\code{\link{gr_model_limits}()},
\code{\link{gr_models}()},
\code{\link{gr_register_model}()},
\code{\link{gr_set_tokenizer}()},
\code{\link{gr_tokenizer}()},
\code{\link{gr_truncate_tokens}()}
}
\concept{cost and token functions}
