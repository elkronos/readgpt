% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-embedders.R
\name{gr_register_embedder}
\alias{gr_register_embedder}
\title{Register an embedding backend}
\usage{
gr_register_embedder(name, fn, description = "", deterministic = FALSE)
}
\arguments{
\item{name}{Short id, used as the value of \code{gr_options(embedder =)} and as
the \code{"embedding_source"} recorded on the result.}

\item{fn}{Function of \verb{(texts, params)} returning a numeric matrix with one
row per input. \code{params} carries \code{client}, \code{model}, \code{batch_size}, \code{cache}
and \code{trace}. Rows should be L2-normalised: everything downstream treats the
cross-product as cosine similarity. Signal failure by raising -- the caller
applies its own \code{fallback} policy, which is not the embedder's business.}

\item{description}{One line, shown by \code{\link[=gr_embedders]{gr_embedders()}}.}

\item{deterministic}{\code{TRUE} if the same text always gives the same vector in
any session on any machine. \strong{Say so only if it is true.} It is what lets a
\code{\link[=gr_replay_client]{gr_replay_client()}} reproduce a run's chunk ranking instead of degrading to
lexical vectors, and claiming it wrongly turns a replay from a recording
into a plausible-looking fiction.}
}
\value{
The name, invisibly.
}
\description{
Embedding is the sixth registry, alongside extractors, cleaners, segmenters,
readers and models. Register a function and every part of the package that
embeds -- the \code{semantic} segmenter, the \code{retrieve} and \code{iterative} readers --
uses it, with no change to any of them.
}
\examples{
# A local model, a company service, anything: it is a function of texts.
gr_register_embedder("first-letters",
  fn = function(texts, params) {
    m <- t(vapply(texts, function(tx) {
      v <- numeric(26)
      ltr <- utf8ToInt(tolower(substr(tx, 1, 40))) - 96L
      for (i in ltr[ltr >= 1 & ltr <= 26]) v[i] <- v[i] + 1
      n <- sqrt(sum(v^2)); if (n == 0) v else v / n
    }, numeric(26), USE.NAMES = FALSE))
    m
  },
  description = "Toy: letter counts", deterministic = TRUE)

gr_embedders()

old <- gr_options(embedder = "first-letters")
attr(gr_embed(gr_client(), c("alpha", "beta")), "embedding_source")
gr_options(old)
}
\seealso{
\code{\link[=gr_embedders]{gr_embedders()}}, \code{\link[=gr_embed]{gr_embed()}}, \code{\link[=gr_options]{gr_options()}} for \code{embedder},
\code{\link[=gr_backend_client]{gr_backend_client()}} to supply an embedder alongside a transport
}
