% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claims.R
\name{gr_outline}
\alias{gr_outline}
\title{Derive a review's sections from its claims}
\usage{
gr_outline(
  claims,
  question = NULL,
  client = NULL,
  model = NULL,
  temperature = NULL,
  max_sections = 6L,
  closing = "What is missing",
  trace = NULL
)
}
\arguments{
\item{claims}{A \code{\link[=gr_claims]{gr_claims()}} result.}

\item{question}{The review question. Taken from \code{claims} if omitted.}

\item{client}{A \code{\link[=gr_client]{gr_client()}}. One is built from \code{model} if omitted.}

\item{model, temperature}{Passed to the model call.}

\item{max_sections}{Upper bound on sections, before the closing one.}

\item{closing}{The heading of the closing section, which receives gap claims
and anything unplaced. \code{NULL} for no closing section.}

\item{trace}{A \code{\link[=gr_trace]{gr_trace()}} to record into.}
}
\value{
A named character vector shaped exactly like the \code{outline} argument of
\code{\link[=gr_synthesise]{gr_synthesise()}} -- headings as names, briefs as values -- carrying
\code{attr(, "claims")} (a \code{section}/\code{claim_id} frame), \code{attr(, "rationale")} and
\code{attr(, "closing")}.
}
\description{
\code{\link[=gr_synthesise]{gr_synthesise()}} takes an \code{outline} fixed before the reading, which makes the
review's structure the author's hypothesis rather than a finding. It is often
the strongest thing a review has to say -- that a literature splits into three
incompatible operationalisations, say, and that the argument about effect size
is really an argument about measurement. This derives the structure from the
claims instead, and hands it back for you to accept or replace.
}
\section{What is verified}{

Every claim is assigned to exactly one section. A claim number that does not
exist is dropped; a claim assigned twice keeps its first section; a claim the
reply never placed is put in the closing section rather than lost, with a
message saying so. A section left with no claims is removed -- except the
closing one, which is allowed to be empty because it is where \code{\link[=gr_gaps]{gr_gaps()}}
writes and gaps are not claims.
}

\examples{
tab <- data.frame(document = c("a.pdf", "b.pdf"), status = "ok",
                  duplicate_of = NA_character_, n_filled = 1L, n_unverified = 0L,
                  conflicts = NA_character_, finding = c("supports", "contradicts"),
                  stringsAsFactors = FALSE)
cl <- gr_mock_client(function(messages, params) {
  if (grepl("<claims>", paste(unlist(messages), collapse = " "), fixed = TRUE)) {
    return(paste0('{"sections":[{"heading":"What it finds","brief":"the claims",',
                  '"claims":[1],"rationale":"one topic"}]}'))
  }
  paste0('{"claims":[{"claim":"It works in trials.","kind":"finding",',
         '"supported_by":[1],"contradicted_by":[2],"moderator":null,"scope":null}]}')
})
cm <- gr_claims(tab, question = "Does it work?", client = cl)
o <- gr_outline(cm, client = cl)
o
attr(o, "claims")
}
\seealso{
\code{\link[=gr_claims]{gr_claims()}}, \code{\link[=gr_synthesise]{gr_synthesise()}}, \code{\link[=gr_gaps]{gr_gaps()}}
}
