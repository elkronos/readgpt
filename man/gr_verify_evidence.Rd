% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-verify.R
\name{gr_verify_evidence}
\alias{gr_verify_evidence}
\title{Check that quoted evidence really is in the document}
\usage{
gr_verify_evidence(answer, chunks = NULL)
}
\arguments{
\item{answer}{A \link{gr_answer}.}

\item{chunks}{The \link{gr_chunks} the answer was read from. Needed for readers
whose evidence is verbatim, where the comparison is against the chunk the
span claims to come from: its \code{source_text} where the segmenter recorded
one (the document text behind a chunk whose \code{text} carries model-written
context or propositions), and its \code{text} otherwise. \code{skim} answers
already carry their sources, so they can be checked without it; an
\code{ensemble} needs it for the rows
its verbatim members contributed, even though its \code{skim} rows do not.

Pass the chunks the answer was actually read from. Chunk ids are positional,
so a \emph{different} chunk set of the same size will match on id and compare
each span against unrelated text, reporting \code{verified = FALSE} for evidence
that is perfectly sound. An id the chunk set does not contain reports \code{NA},
because there was nothing to compare against.}
}
\value{
A data frame with one row per evidence span: \code{chunk_id}, \code{kind}
(\code{"verbatim"}, \code{"extracted"} or \code{"answer"}, \strong{per row}; an \code{ensemble}
mixes them in one table, and the same value appears as the \code{kind} column on
\code{ans$evidence} itself), \code{verified},
\code{match} and \code{span} (the first 60 characters). \code{verified} is \code{NA} where the
question does not apply: a \code{map_reduce} evidence row is a per-chunk
\emph{answer}, not a quotation, and asking whether it appears in the chunk is a
category error.

For an \code{extract} answer, a quote has to carry the value it is cited for as
well as appear in the chunk, and only the reader can check the first: it
knows the value. A row the reader marked \code{verified = FALSE} for that
reason stays \code{FALSE} here, with a \code{match} of 1 when the sentence itself is
in the document. It is there, and it does not say this.
}
\description{
\code{ans$evidence} says what an answer rests on. For most readers those spans are
verbatim chunk text and are true by construction. For \code{skim} they are what
the model chose to write when asked to extract the relevant passages. They
are \emph{presented} as quotations, and this is what checks that they are.
}
\details{
A fabricated citation is more convincing than a fabricated answer, because it
looks like the thing that would let you check. Verification is a string
operation on text you already have, so it costs nothing and there is no
reason not to do it.
}
\section{What the numbers mean}{

\code{match} is 1 for an exact quotation once whitespace, quote marks, dashes and
case are folded away. These are the differences a faithful quotation introduces.
It has to match whole words and whole numbers: "5\%" is not found in "25\%",
nor "12\%" in "-12\%", nor "20" in "200". A span made of several passages (on
separate lines, as a list, or joined by "..." or "[...]") is checked passage
by passage and is verified when every passage is found.
Below 1 it is the fraction of the span's words carried by its longest
consecutive \strong{run} in the source, for the passage that matches worst.

Read that number with its shape in mind. Because it measures a run, \emph{where}
the change falls matters as much as how much changed: altering the last word
of a ten-word span leaves a run of nine and scores 0.9, while altering a word
in the middle splits the span and scores about 0.5. So a mid-sentence change
(a swapped figure, the case this exists to catch) lands near 0.5, not
near 0.9. Below roughly 0.3 there is no quotation left at all, only shared
vocabulary. A run measure is still the right one: word overlap cannot tell a
quotation from a paraphrase assembled out of the same words.
}

\section{Citations}{

With \code{cite = TRUE} a reader asks the model to mark its sources as
\verb{[chunk 3]}. Every answer is checked for citations pointing at chunks that
were never sent, whatever this function is called with; the result is
\code{ans$notes$cited_unknown}, and an answer carrying one is \code{partial}. Lists
and ranges are read (\verb{[chunks 1, 2, and 9]}, \verb{[chunks 1-9]}), and a bracket
that opens like a citation but cannot be read, such as \verb{[chunk nine]}, is
listed in \code{ans$notes$cited_unparsed} and makes the answer \code{partial} too.
}

\examples{
# A model that quotes faithfully.
honest <- gr_mock_client(function(messages, params) {
  txt <- messages[[length(messages)]]$content
  if (grepl("You extract evidence", messages[[1]]$content, fixed = TRUE)) {
    return("Revenue rose to 45.2 million dollars.")
  }
  "Revenue was 45.2 million dollars."
})

doc <- "Revenue rose to 45.2 million dollars.\n\nHeadcount grew to 1,204."
ch <- gr_segment(gr_ingest(doc), list(method = "paragraph", max_tokens = 40))
ans <- gr_read(ch, "What was revenue?", honest, "skim")
gr_verify_evidence(ans)

# A model that invents one. The span is fluent, plausible, and not in the
# document. That is exactly the case a reader cannot catch by eye.
liar <- gr_mock_client(function(messages, params) {
  if (grepl("You extract evidence", messages[[1]]$content, fixed = TRUE)) {
    return("Revenue rose to 88.9 billion dollars on record demand.")
  }
  "Revenue was 88.9 billion dollars."
})
bad <- gr_read(ch, "What was revenue?", liar, "skim")
gr_verify_evidence(bad)
bad$partial
}
\seealso{
\link{gr_answer}, \code{\link[=gr_read]{gr_read()}}, \code{\link[=is_not_found]{is_not_found()}}
}
