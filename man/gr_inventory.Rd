% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory.R
\name{gr_inventory}
\alias{gr_inventory}
\title{What is in a folder, before you read any of it}
\usage{
gr_inventory(
  sources,
  recursive = TRUE,
  model = NULL,
  ocr_min_chars = 40L,
  max_pdf_pages = 3L
)
}
\arguments{
\item{sources}{A directory, or a character vector of paths. A directory is
walked; anything else is taken as given.}

\item{recursive}{Descend into subdirectories. \code{TRUE} here, unlike
\code{\link[=gr_read_many]{gr_read_many()}}, because the point of the function is to show you
everything.}

\item{model}{Model id used for the cost floor. Defaults to
\code{gr_options("model")}.}

\item{ocr_min_chars}{A PDF page with fewer than this many characters of
extractable text is counted as needing OCR. The same default
\code{\link[=gr_ingest_spec]{gr_ingest_spec()}} uses, so what this predicts is what ingestion will do.}

\item{max_pdf_pages}{Pages sampled per PDF for the text-layer probe. The
whole point is to be fast on a big folder; a scan is obvious from a few
pages. \code{Inf} reads every page.}
}
\value{
An object of class \code{gr_inventory}:
\describe{
\item{\code{files}}{One row per file found, readable or not: \code{file} (the path
relative to \code{sources}, so the folder it came from survives), \code{folder},
\code{ext}, \code{bytes}, \code{extractor} (\code{NA} when none claims it), \code{status},
\code{pages}, \code{ocr_pages}, \code{tokens} and \code{note}.}
\item{\code{by_status}}{Counts and sizes per status.}
\item{\code{totals}}{Files, readable files, bytes, known \code{tokens},
\code{tokens_unknown} (readable files whose size cannot be counted yet), and
\code{cost_floor_usd}.}
\item{\code{root}}{The directory surveyed, or \code{NA}.}
}

\code{status} is one of \code{"ready"} (an extractor claims it and there is text),
\code{"needs_ocr"} (a PDF whose pages have no text layer), \code{"needs_package"} (an
extractor claims it, but that extractor's package is not installed, so
reading it would abort), \code{"no_extractor"},
\code{"empty"} (zero bytes, or nothing that reads as text -- \code{note} says which),
or \code{"unreadable"} (it exists but could not be opened, or probing it raised
an error; \code{note} carries the reason).

No file can stop the survey. A folder is surveyed because nobody knows what
is in it yet, so a corrupt PDF, a binary file wearing a \code{.txt} extension, a
broken symlink or anything else unforeseen becomes a row saying so, not an
error. That is the same contract \code{\link[=gr_read_many]{gr_read_many()}} gives a corpus run.
}
\description{
A deterministic survey of a directory or a set of paths: what is there, which
files a registered extractor can read, which PDFs are scans that need OCR,
how big the whole thing is, and roughly what one pass would cost. It makes no
model calls and needs no API key.
}
\details{
Run it before \code{\link[=gr_read_many]{gr_read_many()}} or \code{\link[=gr_extract]{gr_extract()}} on anything you have not
read before. The three things it exists to catch are the three that turn a
corpus run into a confident wrong answer: files no extractor claims (which
would otherwise be dropped without appearing anywhere), PDFs with no text
layer (which extract to nothing and then answer \code{NOT_IN_DOCUMENT},
indistinguishable from a document that genuinely does not say), and a folder
whose contents are one level further down than you scanned.
}
\section{What it does not do}{

It does not decide anything for you. There is no automatic routing of files
to recipes here, and that is deliberate: a router that quietly reads one
document with \code{retrieve} and another with \code{stuff} gives you a plausible
answer built on part of a file with nothing saying so, and it makes
\code{gr_compare()} meaningless because the corpus no longer had \emph{a}
configuration. Group the rows yourself -- \code{split(inv$files, inv$files$folder)}
is usually all it takes -- and pass each group to the recipe you chose.
}

\section{Tokens, and what is left unknown}{

\code{tokens} is counted exactly where counting is cheap -- plain text, markdown,
HTML, CSV, and PDFs from the pages actually probed, scaled by page count. For
formats needing an optional package that is not installed, and for scans
whose text does not exist until OCR runs, it is \code{NA} -- not a guess. Those
files are counted in \code{totals$tokens_unknown} rather than folded into the sum
as zeroes, so the total is always a floor and always says how far from
complete it is.

\code{cost_floor_usd} is a floor: what a single call per document over that much
input would cost. Every per-chunk reader costs more, most of them by a factor
of the chunk count. It is there to catch the order of magnitude -- to tell
four dollars from four hundred -- not to be a quote.
}

\examples{
d <- tempfile(); dir.create(file.path(d, "2019"), recursive = TRUE)
writeLines("The 2019 cohort had 482 participants.", file.path(d, "2019", "report.txt"))
writeLines("notes", file.path(d, "notes.doc"))   # no extractor claims .doc

inv <- gr_inventory(d)
inv
inv$files[, c("file", "folder", "ext", "status", "tokens")]
}
\seealso{
\code{\link[=gr_read_many]{gr_read_many()}}, \code{\link[=gr_extract]{gr_extract()}}, \code{\link[=gr_extractors]{gr_extractors()}},
\code{\link[=gr_ingest_spec]{gr_ingest_spec()}} for the OCR settings this predicts

Other corpus functions: 
\code{\link{gr_calibrate}()},
\code{\link{gr_records}()},
\code{\link{gr_reference}()},
\code{\link{gr_search}()}
}
\concept{corpus functions}
