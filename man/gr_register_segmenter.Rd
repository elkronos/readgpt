% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment-methods.R
\name{gr_register_segmenter}
\alias{gr_register_segmenter}
\title{Register a segmentation strategy}
\usage{
gr_register_segmenter(
  name,
  fn,
  description = "",
  cost = c("free", "embedding", "llm"),
  needs_client = FALSE
)
}
\arguments{
\item{name}{Segmenter name, used in specs and recipes. Re-registering an
existing name replaces it.}

\item{fn}{Function of \verb{(doc, spec, client, trace)} returning a \code{gr_chunks}.
Build the return value with \code{\link[=new_chunks]{new_chunks()}} -- \code{\link[=gr_segment]{gr_segment()}} rejects
anything else. \code{doc} is a \link{gr_document}; \code{spec} carries \code{max_tokens},
\code{overlap_tokens} and \code{min_tokens}, which \link[=new_chunks]{pack_units-style}
helpers respect for you.}

\item{description}{One-line description, shown by \code{\link[=gr_segmenters]{gr_segmenters()}}.}

\item{cost}{\code{"free"}, \code{"embedding"} or \code{"llm"} -- what one run spends, so a
UI can warn before it is spent.}

\item{needs_client}{Whether the segmenter requires a client. \code{\link[=gr_segmenters]{gr_segmenters()}}
reports it, so a UI can check before offering the strategy. When \code{TRUE} and
no client is supplied, \code{\link[=gr_segment]{gr_segment()}} warns with class
\code{"gr_segment_fallback"} before calling \code{fn} -- unless your \code{fn} emits its own
fallback warning, which is better, because it can name what it fell back
\emph{to}. Record the downgrade in the returned \code{method} (\code{"mine->paragraph"})
so it survives into \code{gr_chunk_stats()}.}
}
\value{
Invisibly, \code{name}.
}
\description{
Axis 2 is a registry, so "where does meaning break in this document?" is a
question you can answer for your own material rather than choosing from a
fixed list. A registered segmenter is a first-class one: it appears in
\code{\link[=gr_segmenters]{gr_segmenters()}}, can be named in a \code{\link[=gr_recipe]{gr_recipe()}}, and goes through the same
token-cap enforcement and reporting as the built-ins.
}
\examples{
# One chunk per bullet list. Build the result with the same helper the
# built-ins use, so the token cap and reporting still apply.
gr_register_segmenter("by_bullet", description = "one chunk per bullet",
  fn = function(doc, spec, client, trace) {
    units <- unlist(strsplit(doc$text, "\n(?=[-*])", perl = TRUE))
    new_chunks(units, "by_bullet", spec)
  })
subset(gr_segmenters(), name == "by_bullet")
}
\seealso{
\code{\link[=new_chunks]{new_chunks()}} to build the return value, \code{\link[=gr_segmenters]{gr_segmenters()}},
\code{\link[=gr_segment]{gr_segment()}}, \code{\link[=gr_segment_spec]{gr_segment_spec()}}, \code{\link[=gr_recipe]{gr_recipe()}}

Other segmentation functions: 
\code{\link{gr_chunk_stats}()},
\code{\link{gr_chunks}},
\code{\link{gr_segment}()},
\code{\link{gr_segment_spec}()},
\code{\link{gr_segmenters}()},
\code{\link{new_chunks}()}
}
\concept{segmentation functions}
