% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{gr_records}
\alias{gr_records}
\title{Read a search export, and say what the search found}
\usage{
gr_records(
  exports,
  files = NULL,
  search = NULL,
  dedupe = c("doi+title", "doi", "none")
)
}
\arguments{
\item{exports}{Paths to \code{.ris}, \code{.txt}, \code{.bib} or \code{.bibtex} files, or a
directory containing them. Several exports from several databases is the
normal case and is what the duplicate counts are for.}

\item{files}{A directory of documents, or a character vector of paths, to
match records against. Optional: a record set is useful before anything has
been downloaded.}

\item{search}{A \code{\link[=gr_search]{gr_search()}} describing how the export was produced. Not
required, and the reason to supply it is that a review has to report it.}

\item{dedupe}{\code{"doi"} matches on DOI alone; \code{"doi+title"} (the default) falls
back to normalised title and year when a DOI is missing, which is what
catches the same conference paper indexed twice; \code{"none"} keeps everything.}
}
\value{
An object of class \code{gr_records}:
\describe{
\item{\code{records}}{One row per distinct work, with \code{duplicate_of} naming the
row a dropped record repeats, \code{file} the document matched to it, and
\code{retrieved} whether one was found.}
\item{\code{counts}}{Identified, per database, duplicates removed, distinct
records, reports sought, reports retrieved, reports not retrieved.}
\item{\code{search}}{The \code{gr_search()}, or \code{NULL}.}
\item{\code{unmatched_files}}{Documents on disk that no record claims — usually
a sign the export and the folder are out of step.}
}
}
\description{
Reads RIS or BibTeX exports from one or more databases, removes the records
that are the same work, and matches what is left to the documents you have on
disk. It is the step before \code{\link[=gr_screen]{gr_screen()}}, and it is where a review's numbers
come from: how many records were identified, how many duplicates went, how
many reports were sought and how many were never obtained.
}
\details{
Nothing here calls a model. Which paper a record is, and whether two records
are one paper, are questions a DOI answers exactly.
}
\section{Why start here rather than at a folder}{

A folder of PDFs cannot say which databases were searched, with what query,
on what date, or how many records came back — PRISMA items 6, 7 and 16 — and
no care further down substitutes for them. It also cannot say what is
\emph{missing}: a record with no PDF is "report not retrieved", which is a finding
about the review, and a folder represents it as nothing at all.

It fixes something quieter too. \code{\link[=gr_synthesise]{gr_synthesise()}} cites by author and year,
and without an export those come from asking a model to read a title page —
the one part of a citation that must be exactly right, resting on the loosest
guarantee in the pipeline. From an export they are data.
}

\examples{
ris <- tempfile(fileext = ".ris")
writeLines(c("TY  - JOUR", "AU  - Smith, J.", "TI  - A trial of spacing",
             "PY  - 2019", "DO  - 10.1000/abc", "ER  - "), ris)
recs <- gr_records(ris)
recs
recs$records[, c("authors", "year", "title", "doi", "retrieved")]
}
\seealso{
\code{\link[=gr_search]{gr_search()}}, \code{\link[=gr_screen]{gr_screen()}}, \code{\link[=gr_flow]{gr_flow()}}, \code{\link[=gr_inventory]{gr_inventory()}}

Other corpus functions: 
\code{\link{gr_calibrate}()},
\code{\link{gr_inventory}()},
\code{\link{gr_reference}()},
\code{\link{gr_search}()}
}
\concept{corpus functions}
