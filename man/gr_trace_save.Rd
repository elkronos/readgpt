% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-replay.R
\name{gr_trace_save}
\alias{gr_trace_save}
\title{Save a trace to a file}
\usage{
gr_trace_save(trace, path)
}
\arguments{
\item{trace}{A \code{gr_trace}.}

\item{path}{Destination file.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the trace as JSON, in the form \code{\link[=as_json]{as_json()}} produces. The file is
everything \code{\link[=gr_replay_client]{gr_replay_client()}} needs, so this is how a run leaves the
session it happened in.
}
\examples{
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")
ans <- answer_document(readgpt_example(), "What was revenue?", "fast", client = cl)

f <- tempfile(fileext = ".json")
gr_trace_save(ans$trace, f)
file.exists(f)
}
\seealso{
\code{\link[=gr_replay_client]{gr_replay_client()}}, \code{\link[=as_json]{as_json()}}, \code{\link[=gr_trace]{gr_trace()}}
}
