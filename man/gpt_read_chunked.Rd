% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{gpt_read_chunked}
\alias{gpt_read_chunked}
\title{Deprecated: chunk-by-chunk reading}
\usage{
gpt_read_chunked(chunks, question, client = NULL, return_json = FALSE, ...)
}
\arguments{
\item{chunks}{Character vector of chunks, or a \code{gr_chunks}.}

\item{question}{The question.}

\item{client}{A \code{gr_client}.}

\item{return_json}{Return the whole answer object, including its trace, as JSON.}

\item{...}{Passed to \code{gr_read_spec()}.}
}
\value{
A single string when \code{return_json} is \code{FALSE}; otherwise a
\code{json}-classed string holding the answer, its notes and the full trace.
}
\description{
Superseded by \code{\link[=gr_read]{gr_read()}} with \code{reader = "map_reduce"}. Kept so v1 scripts
keep running; it warns once per session and does \strong{not} reproduce v1's
unbounded merge prompt, which produced HTTP 400s once the per-chunk answers
outgrew the context window. For v1's actual behaviour, use
\code{recipe = "legacy_v1"}.
}
\examples{
cl <- gr_mock_client(function(m, p) "45.2 million dollars")
chunks <- suppressWarnings(parse_text(readgpt_example(), chunk_token_limit = 200))

# v1 style, still works, warns once.
suppressWarnings(gpt_read_chunked(chunks, "What was revenue?", client = cl))

# The modern equivalent, which also reports what it did.
ch <- gr_segment(readgpt_example(), list(method = "paragraph", max_tokens = 200))
gr_read(ch, "What was revenue?", gr_mock_client(function(m, p) "45.2 million dollars"),
        "map_reduce")$notes$chunks
}
\seealso{
\code{\link[=gr_read]{gr_read()}} and \code{reader = "map_reduce"}, \code{\link[=gr_readers]{gr_readers()}},
\code{\link[=answer_document]{answer_document()}}, \code{\link[=gr_recipes]{gr_recipes()}} for \code{"legacy_v1"}

Other v1 compatibility: 
\code{\link{answer_question}()},
\code{\link{gpt_read_hierarchical}()},
\code{\link{gpt_read_multipass}()},
\code{\link{gpt_read_retrieval}()},
\code{\link{parse_text}()}
}
\concept{v1 compatibility}
