% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{gr_read_many}
\alias{gr_read_many}
\title{Ask one question of many documents}
\usage{
gr_read_many(
  sources,
  question,
  recipe = "thorough",
  client = NULL,
  store = NULL,
  on_error = c("continue", "stop"),
  max_total_usd = NULL,
  keep_answers = TRUE,
  recursive = FALSE,
  ...
)
}
\arguments{
\item{sources}{A character vector of file paths, or a single directory, or
raw text. A directory is expanded to the files in it whose extensions any
registered extractor claims -- so which files are picked up follows
\code{\link[=gr_extractors]{gr_extractors()}}, including any you registered yourself.}

\item{question}{The question, asked of every document.}

\item{recipe}{One recipe, applied to every document.}

\item{client}{A \code{gr_client}. Wrap it in \code{\link[=gr_cache_client]{gr_cache_client()}} for a long run:
with a durable cache directory, a restart pays for nothing it has already
answered.}

\item{store}{Optional directory. Each document's result is written there as
it completes and restored on a later run instead of being read again. This
is what makes a four-hour run survive being interrupted.}

\item{on_error}{\code{"continue"} (default) records the failure and moves on;
\code{"stop"} aborts. One unreadable file in two hundred should not cost you the
other hundred and ninety-nine.}

\item{max_total_usd}{Stop once the run has spent this much, marking the
remaining documents \code{"skipped"}. This is a \emph{corpus} ceiling and is separate
from \code{gr_options(max_cost_usd =)}, which is a per-document pre-flight check.
It needs a model with a registered price: against one without, cost is
\emph{unknown} rather than zero, the ceiling cannot be enforced, and you get a
\code{gr_corpus_cost_unknown} warning instead of a silent free pass.}

\item{keep_answers}{Keep every \link{gr_answer} in the result. Set \code{FALSE} for a
large corpus, where holding every trace and evidence table is the thing that
runs you out of memory.}

\item{recursive}{Descend into subdirectories when \code{sources} is a directory.}

\item{...}{Overrides applied to the recipe, as in \code{\link[=answer_document]{answer_document()}}.}
}
\value{
An object of class \code{gr_corpus}: \code{summary} (one row per document),
\code{answers} (named list, empty when \code{keep_answers = FALSE}), \code{trace} (every
call made \emph{this run}) and \code{store}.
}
\description{
The counterpart to \code{\link[=gr_compare]{gr_compare()}}: that runs several recipes over one
document, this runs one recipe over many. Returns one tidy row per document,
so the result goes straight into a data frame you can write out, join, or
code against.
}
\section{The summary}{

\code{document}, \code{answer}, \code{not_found}, \code{partial}, \code{reader}, \code{chunks},
\code{chunks_used}, \code{calls}, \code{cached}, \code{tokens_in}, \code{tokens_out}, \code{cost_usd},
\code{seconds}, \code{status}, \code{error}.

\code{status} is \code{"ok"}, \code{"failed"}, \code{"skipped"} (the corpus ceiling was reached
first) or \code{"restored"} (read from \code{store}, not re-read now). A restored row
keeps the numbers from when that document was first read, so its \code{cost_usd}
is what it cost then, not what this run spent -- which is why the run's own
spend comes from \code{gr_trace_cost(x$trace)} and not from summing the column.
}

\section{Budgets}{

Every document gets its own trace, so \code{gr_options(max_calls =)} and
\code{gr_options(max_cost_usd =)} apply per document exactly as they would if you
read it alone. One enormous document therefore cannot starve the rest. The
corpus-wide ceiling is \code{max_total_usd}.
}

\section{What this does not do}{

It reads documents one at a time. Per-document work is embarrassingly
parallel, but a \code{gr_trace} accumulates by reference and does not survive
being sent to a worker process, so parallelising it would silently lose the
accounting that is half the point.
}

\examples{
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")

a <- tempfile(fileext = ".txt"); writeLines("Revenue was 45.2 million.", a)
b <- tempfile(fileext = ".txt"); writeLines("Revenue was 51.8 million.", b)

out <- gr_read_many(c(a, b), "What was revenue?", "fast", client = cl)
out$summary[, c("document", "answer", "not_found", "status")]

# A missing file is one bad row, not a failed run.
bad <- gr_read_many(c(a, "no-such-file.txt"), "What was revenue?", "fast", client = cl)
bad$summary[, c("document", "status", "error")]

# What the run actually cost, counting only calls that were really issued.
gr_trace_cost(out$trace)
}
\seealso{
\code{\link[=answer_document]{answer_document()}}, \code{\link[=gr_compare]{gr_compare()}}, \code{\link[=gr_cache_client]{gr_cache_client()}},
\code{\link[=gr_trace_cost]{gr_trace_cost()}}
}
