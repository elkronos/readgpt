% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{answer_question}
\alias{answer_question}
\title{Deprecated: answer a question using v1 mode names}
\usage{
answer_question(
  file_path,
  question,
  mode = "Chunked",
  use_parallel = FALSE,
  refine = FALSE,
  return_json = FALSE,
  client = NULL,
  ...
)
}
\arguments{
\item{file_path}{Path to the document.}

\item{question}{The question.}

\item{mode}{One or more of \code{"Retrieval"}, \code{"Chunked"}, \code{"Semantic"},
\code{"Hierarchical"}, \code{"MultiPass"}. Defaults to \code{"Chunked"} only.}

\item{use_parallel}{Run per-chunk calls in parallel.}

\item{refine}{Request chunk-level citations in the answer. v1's \code{refine}
verification pass is not reproduced -- it could never run, because it
called a \code{search_text()} function that was never defined.}

\item{return_json}{Return the answers, the comparison summary and the trace
as JSON instead of the answer string.}

\item{client}{A \code{gr_client}.}

\item{...}{Passed to \code{answer_document()}.}
}
\value{
A string, or a named list of strings when several modes are given.
}
\description{
Maps the old \code{mode} strings onto recipes. Unlike v1, each mode runs as an
isolated pipeline, so selecting several modes no longer changes any of their
answers, and \code{mode} no longer defaults to running all five.
}
\examples{
cl <- gr_mock_client(function(m, p) "45.2 million dollars")

# v1 style, still works, warns once.
suppressWarnings(
  answer_question(gptread_example(), "What was revenue?", mode = "Chunked", client = cl))

# The modern equivalent.
answer_document(gptread_example(), "What was revenue?", "thorough",
                client = cl, return = "text")
}
\seealso{
\code{\link[=answer_document]{answer_document()}}, \code{\link[=gr_compare]{gr_compare()}}, \code{\link[=gr_recipes]{gr_recipes()}}

Other v1 compatibility: 
\code{\link{gpt_read_chunked}()},
\code{\link{gpt_read_hierarchical}()},
\code{\link{gpt_read_multipass}()},
\code{\link{gpt_read_retrieval}()},
\code{\link{parse_text}()}
}
\concept{v1 compatibility}
