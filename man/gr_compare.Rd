% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/answer.R
\name{gr_compare}
\alias{gr_compare}
\title{Run several recipes over one document and compare them}
\usage{
gr_compare(
  source,
  question,
  recipes = c("fast", "needle", "thorough"),
  client = NULL,
  allow_duplicates = FALSE,
  on_error = c("continue", "stop"),
  ...
)
}
\arguments{
\item{source}{File path or raw text.}

\item{question}{The question.}

\item{recipes}{A character vector of recipe names, or a list of \code{gr_recipe}s.}

\item{client}{A \code{gr_client}.}

\item{allow_duplicates}{Run duplicates anyway (useful at temperature > 0 to
measure variance).}

\item{on_error}{\code{"continue"} keeps going and records the failure;
\code{"stop"} aborts the whole comparison.}

\item{...}{Overrides applied to every recipe.}
}
\value{
A list with \code{answers} (named list of \link{gr_answer}), \code{summary} (a data
frame with columns \code{recipe}, \code{segmenter}, \code{chunks}, \code{reader}, \code{signature},
\code{partial}, \code{chunks_used}, \code{answer_chars}, \code{not_found}, \code{error}), \code{trace}
(shared across all recipes, so it records every recipe's calls) and
\code{document} (source and ingestion stats).
}
\description{
Each recipe is an independent pipeline, so a recipe's answer is identical
whether it is run alone or alongside others. Extraction is shared through the
ingest cache, and segmentation is shared between recipes whose segment specs
are identical -- so comparing five readers over one chunking costs one
chunking, not five.
}
\details{
Recipes that resolve to identical ingestion, identical segmentation \emph{and} an
identical read spec are collapsed with a warning rather than billed twice. A
shared reader signature alone is not enough: two \code{retrieve} recipes with
different \code{top_k} share a signature and are genuinely different runs.
}
\examples{
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")

# Three pipelines over one document. Extraction is shared, so this costs one
# extraction, not three; only the segmentation and the reader vary.
cmp <- gr_compare(readgpt_example(), "What was revenue?",
                  c("fast", "precise", "needle"), client = cl)
cmp$summary[, c("recipe", "segmenter", "chunks", "reader", "signature", "chunks_used")]

# One trace covers all three, so this is the cost of the whole comparison.
gr_trace_summary(cmp$trace)
}
\seealso{
\code{\link[=answer_document]{answer_document()}}, \code{\link[=gr_recipes]{gr_recipes()}}, \code{\link[=gr_recipe]{gr_recipe()}}, \link{gr_answer}
}
