% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-embed.R
\name{gr_embed}
\alias{gr_embed}
\title{Embed texts}
\usage{
gr_embed(
  client,
  texts,
  model = NULL,
  batch_size = 64L,
  cache = NULL,
  trace = NULL,
  fallback = c("lexical", "error", "none")
)
}
\arguments{
\item{client}{A \code{gr_client}.}

\item{texts}{Character vector.}

\item{model}{Embedding model id; defaults to the client's.}

\item{batch_size}{Texts per request.}

\item{cache}{Use the session embedding cache.}

\item{trace}{Optional trace.}

\item{fallback}{What to do when the embedding request fails. \strong{Defaults to
\code{"lexical"}}: hashed bag-of-words vectors that measure word overlap, not
meaning, so \code{semantic} segmentation and \code{retrieve} ranking become markedly
less accurate. The substitution warns and is recorded, but the run
continues. Use \code{"error"} to fail fast, or \code{"none"} to get an empty matrix.}
}
\value{
A numeric matrix, one row per input, carrying an \code{"embedding_source"}
attribute of \code{"api"} or \code{"lexical"} -- always check it before treating the
rows as semantic. Rows from the API and lexical paths are L2-normalised.
With \code{fallback = "none"} and a failed request the result is a 0 x 0 matrix.
}
\description{
Embed texts
}
\examples{
cl <- gr_mock_client()
e <- gr_embed(cl, c("cats sleep all day", "dogs bark all night",
                    "revenue rose to 45.2 million"))

# Always check this before treating the rows as semantic: on the lexical
# fallback they reflect word overlap, not meaning.
attr(e, "embedding_source")

# Rows are L2-normalised, so the cross-product is cosine similarity.
round(e \%*\% t(e), 3)

# The semantic segmenter records the same thing, so a degraded run stays
# visible after the fact.
gr_segment(readgpt_example(), list(method = "semantic", max_tokens = 200),
           client = cl)$extra$embedding_source
}
\seealso{
\code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_segment_spec]{gr_segment_spec()}} for \code{method = "semantic"},
\code{\link[=gr_read_spec]{gr_read_spec()}} for \code{reader = "retrieve"}, \code{\link[=gr_models]{gr_models()}} for the
embedding models in the registry
}
