% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/records.R
\name{gr_search}
\alias{gr_search}
\title{Record how the search was run}
\usage{
gr_search(
  databases,
  dates = NULL,
  limits = NULL,
  registration = NA_character_,
  other = NULL,
  notes = NULL
)
}
\arguments{
\item{databases}{Named character vector or list: name of each source, value
the query string run against it. The query is the point; a review reporting
"we searched PubMed" without it cannot be repeated.}

\item{dates}{When each search was run, as \code{YYYY-MM-DD}. One value, or one per
database. Searches drift: a review is a statement about a date.}

\item{limits}{Limits applied — language, publication years, study design
filters. Free text; a review states them whatever they were.}

\item{registration}{Registration identifier (a PROSPERO number, say), or
\code{NA} and say so. PRISMA item 24 asks either way.}

\item{other}{Any further sources: reference lists checked, experts contacted,
registers, preprint servers, hand-searched journals.}

\item{notes}{Anything else the methods section has to say.}
}
\value{
An object of class \code{gr_search}.
}
\description{
The part of a review that no tool can infer and every review must state:
which databases were searched, with what query, on what date, and what limits
were applied. PRISMA items 6 and 7 ask for it, and item 7 asks for the full
strategy for at least one database \emph{so that it could be repeated}.
}
\details{
This function computes nothing. It exists so the answer travels with the run
instead of living in a lab notebook — it reaches \code{\link[=gr_flow]{gr_flow()}} and the audit
report, and it round-trips through \code{\link[=gr_protocol_save]{gr_protocol_save()}} alongside the
criteria, so what was searched and what was eligible are one artifact.
}
\examples{
gr_search(
  databases = c(PubMed = "(spaced practice[tiab]) AND (retention[tiab])",
                Scopus = "TITLE-ABS-KEY(\"spaced practice\" AND retention)"),
  dates = "2026-02-14",
  limits = "English; 2000 onwards; primary studies only",
  registration = "PROSPERO CRD42026000000",
  other = "Reference lists of included studies hand-searched"
)
}
\seealso{
\code{\link[=gr_records]{gr_records()}}, \code{\link[=gr_protocol]{gr_protocol()}}, \code{\link[=gr_flow]{gr_flow()}}

Other corpus functions: 
\code{\link{gr_calibrate}()},
\code{\link{gr_inventory}()},
\code{\link{gr_records}()},
\code{\link{gr_reference}()}
}
\concept{corpus functions}
