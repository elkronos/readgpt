% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{gr_calibrate}
\alias{gr_calibrate}
\title{Measure the screener against a hand-screened sample}
\usage{
gr_calibrate(screening, reference, positive = "include", min_positives = 10L)
}
\arguments{
\item{screening}{A \code{gr_screening} from \code{\link[=gr_screen]{gr_screen()}}.}

\item{reference}{The completed frame from \code{\link[=gr_reference]{gr_reference()}}, a path to the
filled-in CSV, or any data frame with \code{document} and \code{human_decision}.}

\item{positive}{Which human decision counts as eligible.}

\item{min_positives}{Below this many eligible studies in the sample, the
sensitivity estimate is reported but marked inadequate.}
}
\value{
An object of class \code{gr_calibration}:
\describe{
\item{\code{counts}}{The confusion matrix, as kept/excluded by eligible/not.}
\item{\code{metrics}}{One row per statistic: \code{estimate}, \code{lower}, \code{upper}, \code{n}.}
\item{\code{missed}}{The eligible studies the screener excluded — the rows
themselves, because a list of the misses says more than a rate.}
\item{\code{disagreements}}{Every row where the two differ, in either
direction.}
\item{\code{adequate}}{Whether the sample supports a sensitivity claim.}
\item{\code{frame}}{Which rows the sample was drawn from, and how many.}
}
}
\description{
Compares \code{\link[=gr_screen]{gr_screen()}}'s decisions with a person's on the same documents, and
reports what that sample supports: how much of the eligible literature the
screener kept, how much of the irrelevant literature it removed, and how much
reading it saved — each with an interval, and each refused when the sample is
too small to say.
}
\section{Two sensitivities, and the gap between them}{

\code{"unclear"} is a deferral, not a miss. A record the screener could not settle
goes to a person, so it is not lost — and counting it as a failure would
punish the screener for the one behaviour that makes it safe.

So two figures are reported. \strong{Sensitivity as deployed} asks what fraction of
the eligible studies survived: kept, whether by \code{"include"} or by
\code{"unclear"}. That is the number that matters, because it is the one where a
shortfall is permanent. \strong{Strict sensitivity} asks what fraction were
actively included. The gap between them is the reading a person still has to
do, and reporting only the first would flatter a screener that defers
everything.
}

\section{Why accuracy is not reported}{

Inclusion rates run at a few per cent, so a screener that excluded every
record would score around 95\% accurate and find nothing. Cohen's kappa is
reported instead, because it is the statistic that notices.
}

\section{What a small sample cannot do}{

Sensitivity is estimated from the eligible studies in the sample and from
nothing else. Twelve hand-screened records containing two eligible studies
estimate it from two observations, and "1.00" from two observations is not a
finding. The intervals are Wilson score intervals, which stay sensible at
zero and one where the textbook interval collapses to a point, and
\verb{$adequate} says whether there was enough to support a claim at all.
}

\examples{
tab <- data.frame(document = paste0("d", 1:8, ".pdf"),
                  decision = c("include", "include", "unclear", "exclude",
                               "exclude", "exclude", "include", "exclude"),
                  stringsAsFactors = FALSE)
ref <- data.frame(document = paste0("d", 1:8, ".pdf"),
                  human_decision = c("include", "exclude", "include", "exclude",
                                     "exclude", "include", "include", "exclude"),
                  stringsAsFactors = FALSE)
gr_calibrate(structure(list(table = tab), class = "gr_screening"), ref)
}
\seealso{
\code{\link[=gr_reference]{gr_reference()}}, \code{\link[=gr_screen]{gr_screen()}}, \code{\link[=gr_audit_report]{gr_audit_report()}}

Other corpus functions: 
\code{\link{gr_inventory}()},
\code{\link{gr_records}()},
\code{\link{gr_reference}()},
\code{\link{gr_search}()}
}
\concept{corpus functions}
