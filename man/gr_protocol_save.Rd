% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{gr_protocol_save}
\alias{gr_protocol_save}
\alias{gr_protocol_read}
\title{Save a protocol to a file, and read one back}
\usage{
gr_protocol_save(protocol, path)

gr_protocol_read(path)
}
\arguments{
\item{protocol}{A \code{\link[=gr_protocol]{gr_protocol()}}.}

\item{path}{File path. \code{gr_protocol_read()} also accepts a JSON string.}
}
\value{
\code{gr_protocol_save()} returns \code{path} invisibly;
\code{gr_protocol_read()} returns a \code{gr_protocol}.
}
\description{
A protocol is meant to be written down before the reading starts, shared with
whoever is checking the work, and cited alongside the results. That means a
file, and JSON because it is exact and needs nothing installed.
}
\details{
The round trip is lossless for everything a protocol \emph{is}. \code{recipe} is the one
thing it may not be: a \code{gr_recipe} object is written as its name, because a
file that pinned every clean and segmentation setting would silently pin them
for a reader on a different version.
}
\examples{
p <- gr_protocols("bibliography")
f <- tempfile(fileext = ".json")
gr_protocol_save(p, f)
identical(gr_protocol_read(f)$question, p$question)
}
\seealso{
\code{\link[=gr_protocol]{gr_protocol()}}, \code{\link[=gr_protocols]{gr_protocols()}}
}
