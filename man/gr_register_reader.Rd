% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{gr_register_reader}
\alias{gr_register_reader}
\title{Register a reading strategy}
\usage{
gr_register_reader(name, fn, signature, description = "", cost_calls = "")
}
\arguments{
\item{name}{Reader name. Re-registering an existing name replaces it.}

\item{fn}{Function of \verb{(chunks, question, client, spec, trace)} returning a
\code{gr_answer}. See the section below.}

\item{signature}{Traversal signature, \code{"select|calls|state"}. Two readers with
the same signature are the same methodology under two names; \code{\link[=gr_compare]{gr_compare()}}
uses it, together with the ingest and segment specs, to decide whether two
recipes are the same experiment, and \code{ensemble} refuses members that share
one.}

\item{description}{One-line description, shown by \code{\link[=gr_readers]{gr_readers()}}.}

\item{cost_calls}{Human-readable call count in terms of N chunks (\code{"N + 1"},
\code{"1 + embeddings"}), shown by \code{\link[=gr_readers]{gr_readers()}}.}
}
\value{
Invisibly, \code{name}.
}
\description{
Axis 3 is a registry, so how the model is made to \emph{read} a chunk set is
yours to define -- and a registered reader is a first-class one: it appears in
\code{\link[=gr_readers]{gr_readers()}}, can be named in a \code{\link[=gr_recipe]{gr_recipe()}} or an \code{ensemble}, and is
subject to the same pre-flight cost and call caps as the built-ins.
}
\section{Writing a reader}{

Your function receives:
\describe{
\item{\code{chunks}}{A \link{gr_chunks}. \code{chunks$chunks} is the data frame:
\code{chunk_id}, \code{text}, \code{tokens}, \code{chars}, \code{page}, \code{section}, \code{block_id}.}
\item{\code{question}}{The question, already validated as non-blank.}
\item{\code{client}}{Pass it to \code{\link[=gr_call]{gr_call()}}; never construct your own.}
\item{\code{spec}}{A \code{\link[=gr_read_spec]{gr_read_spec()}}. Honour at least \code{model},
\code{max_answer_tokens} and \code{temperature}.}
\item{\code{trace}}{Pass it to every \code{\link[=gr_call]{gr_call()}} so your calls are counted, and
check \code{gptread:::trace_can_call(trace)} before each one so the run cap is
respected.}
}
Return \code{\link[=new_answer]{new_answer()}}. Budget your prompt with \code{\link[=gr_budget]{gr_budget()}} rather than
assuming the document fits.
}

\examples{
# A reader that answers from the single longest chunk.
gr_register_reader("longest", signature = "one|1|none", cost_calls = "1",
  description = "answer from the longest chunk only",
  fn = function(chunks, question, client, spec, trace) {
    d <- chunks$chunks
    i <- which.max(d$tokens)
    res <- gr_call(client, list(
      list(role = "user", content = paste0(d$text[i], "\n\nQuestion: ", question))),
      model = spec$model, trace = trace, label = "longest.answer")
    new_answer(res$text, "longest", question, d$chunk_id[i], trace)
  })

ch <- gr_segment(gptread_example(), list(method = "sentence", max_tokens = 120))
gr_read(ch, "What was revenue?", gr_mock_client(function(m, p) "45.2 million"),
        "longest")$answer
}
\seealso{
\code{\link[=new_answer]{new_answer()}} to build the return value, \code{\link[=gr_readers]{gr_readers()}},
\code{\link[=gr_reader_signature]{gr_reader_signature()}}, \code{\link[=gr_read]{gr_read()}}, \link{gr_answer}, \code{\link[=gr_budget]{gr_budget()}}

Other reading functions: 
\code{\link{gr_answer}},
\code{\link{gr_read}()},
\code{\link{gr_read_spec}()},
\code{\link{gr_reader_signature}()},
\code{\link{gr_readers}()},
\code{\link{is_not_found}()},
\code{\link{new_answer}()}
}
\concept{reading functions}
