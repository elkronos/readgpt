% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-trace.R
\name{as_json}
\alias{as_json}
\title{Serialise an object to JSON}
\usage{
as_json(x, pretty = TRUE, ...)
}
\arguments{
\item{x}{Object to serialise. Methods exist for \link{gr_answer}, \code{gr_trace},
\link{gr_chunks} and \link{gr_document}; anything else falls back to a plain
\code{jsonlite} conversion.}

\item{pretty}{Whether to indent.}

\item{...}{Passed to \code{jsonlite::toJSON()}.}
}
\value{
A \code{json}-classed character string. \code{NULL} fields are written as
\code{null} rather than dropped.
}
\description{
A generic so traces, answers, chunk sets and documents all serialise
consistently and safely (\code{auto_unbox} plus \code{null = "null"}, so a missing
field appears as \code{null} rather than vanishing -- assigning \code{NULL} into an R
list \emph{deletes the key}, which is why the old code's \code{final_answer} field
silently disappeared from the JSON whenever a call failed).
}
\examples{
cl <- gr_mock_client(function(m, p) "45.2 million dollars")
ans <- answer_document(gptread_example(), "What was revenue?", "fast", client = cl)

# The answer plus every prompt and response from the same single run.
txt <- as_json(ans)
names(jsonlite::fromJSON(txt))
}
\seealso{
\code{\link[=gr_trace_summary]{gr_trace_summary()}}, \link{gr_answer}
}
