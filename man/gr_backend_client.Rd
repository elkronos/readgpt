% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-backend.R
\name{gr_backend_client}
\alias{gr_backend_client}
\title{Use any function as the model transport}
\usage{
gr_backend_client(
  handler,
  embed = NULL,
  model = "backend-model",
  embedding_model = "backend-embed",
  id = NULL,
  max_retries = 0L,
  timeout = NULL
)
}
\arguments{
\item{handler}{Function of \verb{(messages, params)}. \code{messages} is a list of
\code{list(role=, content=)} with roles \code{"system"}, \code{"developer"}, \code{"user"} or
\code{"assistant"}. \code{params} carries \code{model}, \code{max_output}, \code{temperature},
\code{schema}, \code{schema_name} and \code{prompt_tokens}. Return a single string, or a
\link{gr_result} for full control over token usage and failure reporting.}

\item{embed}{Optional function of \verb{(texts, params)} returning a numeric
matrix with one row per input. Without it, readers that embed
(\code{retrieve}, and the \code{semantic} segmenter) fall back to hashed lexical
vectors and warn.}

\item{model, embedding_model}{Model ids reported to the rest of the package.
If \code{model} is not in the model registry you will get the usual unknown-model
warning and a conservative context window; \code{\link[=gr_register_model]{gr_register_model()}} fixes that,
and getting it right matters because it is what sizes your chunks.}

\item{id}{Stable identity for this backend, used by \code{\link[=gr_cache]{gr_cache()}} and by
\code{\link[=gr_read_many]{gr_read_many()}}'s \code{store}. \strong{Read this before setting up a durable cache.}

For \code{\link[=gr_client]{gr_client()}} a cache key can be built from the API shape, base URL and
model, because those completely describe what will answer: two clients with
the same three give the same answers today and next month, which is what
makes a cache safe to keep on disk. A backend has none of that -- every one
of them is \code{api = "backend"}, \code{base_url = "backend://"} -- and the thing
that actually answers is an R closure, whose behaviour this package cannot
inspect.

So the default is a fresh id per client object, which is \emph{safe}: two
different handlers can never trade answers. It is also \emph{session-scoped}, so
a cache or store will not be reused by a later session. Pass a stable \code{id}
to get cross-session reuse -- and pass one \strong{only} when the handler really
does answer the same way every time, because that is the assertion you are
making. Hashing the closure would not do: two handlers can share a body and
differ in what they captured.}

\item{max_retries, timeout}{Recorded on the client for completeness. Retrying
is the backend's business -- this package does not retry a handler, because
it cannot know whether the failure was transient.}
}
\value{
An object of class \code{gr_backend_client}, usable anywhere a
\code{\link[=gr_client]{gr_client()}} is, with \verb{$calls()} recording every request made through it.
}
\description{
Wraps an arbitrary R function as a client. \code{handler} receives
\verb{(messages, params)} and returns a string or a \link{gr_result}; everything else
the package does -- context budgeting, cost and call caps, provenance, the
run trace, \code{\link[=gr_cache]{gr_cache()}}, \code{\link[=gr_replay_client]{gr_replay_client()}}, \code{\link[=gr_compare]{gr_compare()}} -- works
exactly as it does for the built-in HTTP client.
}
\details{
Use it to reach a provider this package does not speak to, a company proxy, a
local model, or another R package's client. \code{\link[=gr_ellmer_client]{gr_ellmer_client()}} is this
function with an \code{ellmer} chat object plugged in.
}
\section{Failure is data, not an exception}{

A handler that raises is caught and reported as a failed \link{gr_result}, the
same as an HTTP error, so one bad chunk does not abort a run. A handler that
returns \code{""} is treated as an empty completion, which this package reports as
\code{ok = FALSE} rather than passing \code{""} downstream as evidence.
}

\examples{
# Any function will do. This one is deterministic so the example is too.
cl <- gr_backend_client(function(messages, params) {
  user <- Filter(function(m) m$role == "user", messages)
  sprintf("I was asked \%d thing(s) with a \%d-token cap.",
          length(user), params$max_output)
}, model = "my-backend")

gr_call(cl, "What was revenue?", max_output = 128L)$text

# It composes with everything else: caching, tracing, cost accounting.
tr <- gr_trace()
invisible(gr_call(cl, "again", trace = tr))
gr_trace_summary(tr)[c("calls", "tokens_in", "tokens_out")]
}
\seealso{
\code{\link[=gr_ellmer_client]{gr_ellmer_client()}}, \code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_mock_client]{gr_mock_client()}},
\code{\link[=gr_register_model]{gr_register_model()}}, \code{\link[=gr_cache_client]{gr_cache_client()}}
}
