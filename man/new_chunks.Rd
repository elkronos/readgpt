% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment-core.R
\name{new_chunks}
\alias{new_chunks}
\title{Build a \code{gr_chunks}, the object every segmenter must return}
\usage{
new_chunks(
  text,
  method,
  spec,
  page = NA_integer_,
  section = NA_character_,
  block_id = NA_integer_,
  extra = list()
)
}
\arguments{
\item{text}{Character vector of chunk texts. Blank entries are dropped.}

\item{method}{Your segmenter's name. Record a fallback here if you took one
(\code{"semantic->paragraph"}); \code{\link[=gr_chunk_stats]{gr_chunk_stats()}} surfaces it.}

\item{spec}{The \code{gr_segment_spec} passed to your segmenter.}

\item{page, section, block_id}{Provenance, one value per chunk or one recycled
value. Leave as \code{NA} rather than guessing: wrong provenance sends a reader
to the wrong page with full confidence.}

\item{extra}{Named list of segmenter-specific detail, kept on \verb{$extra}.}
}
\value{
A \link{gr_chunks}.
}
\description{
Exported because it is part of the extension API: \code{\link[=gr_segment]{gr_segment()}} rejects
anything that does not inherit \code{"gr_chunks"}, so a custom segmenter
registered with \code{\link[=gr_register_segmenter]{gr_register_segmenter()}} cannot be written without this.
Using it also gets you the shared invariants for free -- blank units dropped,
\code{chunk_id} assigned in order, tokens and characters measured, provenance
recycled to match -- so your segmenter behaves like the built-ins wherever
the rest of the package touches it.
}
\details{
The token cap is NOT enforced here. \code{\link[=gr_segment]{gr_segment()}} checks it after your
function returns and re-splits anything oversized, so a segmenter that
ignores \code{spec$max_tokens} produces a warning and correct chunks rather than
an HTTP 400.
}
\examples{
# One chunk per bullet, with the source block recorded.
doc <- gr_ingest("Findings:\n\n- Revenue rose.\n- Costs fell.\n- Margin widened.")
ch <- new_chunks(trimws(strsplit(doc$text, "\n(?=-)", perl = TRUE)[[1]]),
                 method = "by_bullet", spec = gr_segment_spec(max_tokens = 100),
                 block_id = 1L)
gr_chunk_stats(ch)
}
\seealso{
\code{\link[=gr_register_segmenter]{gr_register_segmenter()}}, \link{gr_chunks}, \code{\link[=gr_segment]{gr_segment()}},
\code{\link[=gr_chunk_stats]{gr_chunk_stats()}}, \code{\link[=new_answer]{new_answer()}}

Other segmentation functions: 
\code{\link{gr_chunk_stats}()},
\code{\link{gr_chunks}},
\code{\link{gr_register_segmenter}()},
\code{\link{gr_segment}()},
\code{\link{gr_segment_spec}()},
\code{\link{gr_segmenters}()}
}
\concept{segmentation functions}
