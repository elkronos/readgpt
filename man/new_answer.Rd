% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-core.R
\name{new_answer}
\alias{new_answer}
\title{Build a \code{gr_answer}, the object every reader must return}
\usage{
new_answer(
  text,
  reader,
  question,
  chunks_used,
  trace,
  evidence = NULL,
  partial = FALSE,
  notes = list()
)
}
\arguments{
\item{text}{The answer string. Use \code{"NOT_IN_DOCUMENT"} when the chunks did
not contain the answer; \code{\link[=gr_compare]{gr_compare()}} counts that separately from a
failure.}

\item{reader}{Your reader's name, as registered.}

\item{question}{The question, carried through for the record.}

\item{chunks_used}{Integer \code{chunk_id}s that CONTRIBUTED to the answer -- not
every chunk you sent.}

\item{trace}{The \code{gr_trace} passed to your reader. Pass it through; do not
create a new one, or your calls will not appear in the run's totals.}

\item{evidence}{Optional data frame of supporting spans; build it with the
columns \code{chunk_id}, \code{text}, \code{page}, \code{section}, \code{score}.}

\item{partial}{\code{TRUE} if anything degraded -- a failed call, a dropped chunk,
a truncated prompt. Callers are told to check this before trusting
\verb{$answer}, so setting it honestly matters more than it looks.}

\item{notes}{Named list of whatever your reader wants to report.}
}
\value{
A \link{gr_answer}.
}
\description{
Exported because it is part of the extension API: \code{\link[=gr_read]{gr_read()}} rejects
anything that does not inherit \code{"gr_answer"}, so a custom reader registered
with \code{\link[=gr_register_reader]{gr_register_reader()}} cannot be written without this. Using it also
means your reader reports \code{partial}, \code{evidence} and \code{notes} the same way the
built-ins do, so \code{\link[=gr_compare]{gr_compare()}} can put it in the same table.
}
\examples{
# The minimum a custom reader has to return.
tr <- gr_trace()
a <- new_answer("Revenue was 45.2 million.", "my_reader", "What was revenue?",
                chunks_used = 1L, trace = tr, notes = list(strategy = "first chunk"))
a$partial
a$notes$strategy
}
\seealso{
\code{\link[=gr_register_reader]{gr_register_reader()}}, \link{gr_answer}, \code{\link[=gr_read]{gr_read()}}, \code{\link[=new_chunks]{new_chunks()}}

Other reading functions: 
\code{\link{gr_answer}},
\code{\link{gr_read}()},
\code{\link{gr_read_spec}()},
\code{\link{gr_reader_signature}()},
\code{\link{gr_readers}()},
\code{\link{gr_register_reader}()},
\code{\link{is_not_found}()}
}
\concept{reading functions}
