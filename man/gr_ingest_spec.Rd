% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{gr_ingest_spec}
\alias{gr_ingest_spec}
\title{Describe an ingestion configuration}
\usage{
gr_ingest_spec(
  clean = "standard",
  ocr = c("auto", "always", "never"),
  ocr_lang = "eng",
  ocr_dpi = 300,
  ocr_min_chars = 40L,
  min_chars = 20L,
  extractor = NULL,
  parallel = NULL,
  cleaner_opts = list()
)
}
\arguments{
\item{clean}{A preset name (\code{"none"}, \code{"minimal"}, \code{"standard"},
\code{"academic"}, \code{"scan"}, \code{"legacy_v1"}) or a character vector of cleaner
names from \code{gr_cleaners()}.}

\item{ocr}{\code{"auto"} (OCR pages with no text layer), \code{"always"}, or \code{"never"}.}

\item{ocr_lang}{Tesseract language code.}

\item{ocr_dpi}{Render density for PDF OCR.}

\item{ocr_min_chars}{A page with fewer characters than this is treated as
needing OCR under \code{ocr = "auto"}.}

\item{min_chars}{Refuse the document if less than this much text survives.}

\item{extractor}{Force a specific extractor name instead of dispatching on
the file extension.}

\item{parallel}{Parallelise page-level OCR.}

\item{cleaner_opts}{Extra options passed to individual cleaners.}
}
\value{
A list of class \code{gr_ingest_spec}.
}
\description{
Describe an ingestion configuration
}
\examples{
# What each preset costs you, on a real document, before any model call.
vapply(c("none", "minimal", "standard", "academic", "scan"),
       function(p) gr_ingest(readgpt_example(), gr_ingest_spec(clean = p))$stats$chars,
       integer(1))

# Or name the steps yourself; see gr_cleaners() for what is available.
gr_ingest_spec(clean = c("page_numbers", "references"), ocr = "never")$clean
}
\seealso{
\code{\link[=gr_ingest]{gr_ingest()}}, \code{\link[=gr_cleaners]{gr_cleaners()}} for the individual step names,
\code{\link[=gr_clean]{gr_clean()}} to preview a configuration on your own text

Other ingest functions: 
\code{\link{gr_clean}()},
\code{\link{gr_cleaners}()},
\code{\link{gr_document}},
\code{\link{gr_extractors}()},
\code{\link{gr_ingest}()},
\code{\link{gr_register_cleaner}()},
\code{\link{gr_register_extractor}()}
}
\concept{ingest functions}
