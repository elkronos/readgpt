% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-client.R
\name{gr_client}
\alias{gr_client}
\title{Construct a model client}
\usage{
gr_client(
  model = NULL,
  api = NULL,
  api_key = NULL,
  base_url = NULL,
  embedding_model = NULL,
  max_retries = NULL,
  retry_pause_base = NULL,
  timeout = NULL,
  extra_body = list()
)
}
\arguments{
\item{model}{Default chat model id.}

\item{api}{\code{"responses"} (default) or \code{"chat"}.}

\item{api_key}{Optional key; resolved lazily at call time if omitted.}

\item{base_url}{API base, for proxies and compatible endpoints.}

\item{embedding_model}{Default embedding model id.}

\item{max_retries, retry_pause_base}{Retry policy for transient failures.}

\item{timeout}{Per-request timeout in seconds.}

\item{extra_body}{Named list merged into every request body.}
}
\value{
An object of class \code{gr_client}: a list of the settings above.
Constructing one makes no request and does not require a key -- the key is
resolved at call time by \code{\link[=gr_api_key]{gr_api_key()}}.
}
\description{
The client is an object, not a global. Passing it explicitly is what lets a
Shiny app serve two users with two different keys in one R process -- the old
code called \code{Sys.setenv(OPENAI_API_KEY = ...)}, which is process-wide, so the
second user's key silently billed the first user's requests.
}
\examples{
# A client is a value, not a global. Two of them, two keys, one R process --
# which is what makes a Shiny app serving two users safe.
a <- gr_client(model = "gpt-4o",  api_key = "sk-user-a")
b <- gr_client(model = "gpt-4.1", api_key = "sk-user-b", timeout = 30)
vapply(list(a = a, b = b), function(cl) cl$model, character(1))

# What that model can actually take.
unlist(gr_model_info(a$model)[c("context_window", "max_output")])

# No key needed to develop against the pipeline.
gr_call(gr_mock_client(function(m, p) "hi there"), "hello")$text

\dontrun{
cl <- gr_client(model = "gpt-5.6-terra")
gr_call(cl, list(list(role = "user", content = "Say hi.")), max_output = 20)
}
}
\seealso{
\code{\link[=gr_call]{gr_call()}} to use it, \code{\link[=gr_mock_client]{gr_mock_client()}} to work offline,
\code{\link[=gr_api_key]{gr_api_key()}} for key resolution, \code{\link[=gr_options]{gr_options()}} for the defaults,
\link{gr_result} for what a call returns
}
