% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment.R
\name{gr_segment_spec}
\alias{gr_segment_spec}
\title{Describe a segmentation configuration}
\usage{
gr_segment_spec(
  method = "paragraph",
  max_tokens = 1200L,
  overlap_tokens = 0L,
  min_tokens = 0L,
  separators = NULL,
  prefix_section = TRUE,
  semantic_window = 2L,
  semantic_percentile = 90,
  context_source = c("metadata", "llm"),
  proposition_batch_tokens = 900L,
  parallel = NULL,
  ...
)
}
\arguments{
\item{method}{Segmenter name; see \code{gr_segmenters()}.}

\item{max_tokens}{Hard cap on chunk size, in tokens. Always enforced -- a
segmenter cannot emit an oversized chunk, which the old \code{chunk_text_semantic()}
routinely did.}

\item{overlap_tokens}{Tokens of trailing context copied into the start of the
next chunk. Overlap is what stops an answer straddling a boundary from being
lost by both chunks. Honoured by every segmenter except \code{page} (a page is
the unit) and \code{proposition} (propositions are already self-contained).}

\item{min_tokens}{A chunk smaller than this is merged \strong{backward} into the
chunk before it. Ignored by \code{page} and \code{fixed}.}

\item{separators}{For \code{method = "recursive"}: the cascade, strongest first.}

\item{prefix_section}{For \code{method = "structural"}: prepend the heading.}

\item{semantic_window, semantic_percentile}{For \code{method = "semantic"}: how many
sentences to embed together, and how extreme a distance counts as a
boundary (higher = fewer, larger chunks).}

\item{context_source}{For \code{method = "contextual"}: \code{"metadata"} (free) or
\code{"llm"} (one call per chunk).}

\item{proposition_batch_tokens}{For \code{method = "proposition"}: batch size.}

\item{parallel}{Parallelise per-chunk model work in segmenters that do any.}

\item{...}{Extra fields for custom segmenters.}
}
\value{
A list of class \code{gr_segment_spec}.
}
\description{
Describe a segmentation configuration
}
\section{Out-of-range values}{

Arguments are clamped into a usable range and the change is warned about,
never applied silently: \code{max_tokens} [32, 1e6], \code{overlap_tokens}
[0, max_tokens - 1], \code{min_tokens} [0, max_tokens], \code{semantic_window}
[1, 10], \code{semantic_percentile} [50, 99.5], \code{proposition_batch_tokens}
[200, 4000].
}

\examples{
gr_segment_spec("semantic", max_tokens = 800, overlap_tokens = 80)$overlap_tokens

# An impossible setting is corrected loudly.
suppressWarnings(gr_segment_spec("paragraph", max_tokens = 100,
                                 overlap_tokens = 500)$overlap_tokens)
}
\seealso{
\code{\link[=gr_segmenters]{gr_segmenters()}} for the available methods and their costs,
\code{\link[=gr_segment]{gr_segment()}}, \code{\link[=gr_chunk_stats]{gr_chunk_stats()}}

Other segmentation functions: 
\code{\link{gr_chunk_stats}()},
\code{\link{gr_chunks}},
\code{\link{gr_register_segmenter}()},
\code{\link{gr_segment}()},
\code{\link{gr_segmenters}()},
\code{\link{new_chunks}()}
}
\concept{segmentation functions}
