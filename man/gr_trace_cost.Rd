% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{gr_trace_cost}
\alias{gr_trace_cost}
\title{What a run actually cost}
\usage{
gr_trace_cost(trace)
}
\arguments{
\item{trace}{A \code{gr_trace}.}
}
\value{
A data frame with one row per model: \code{model}, \code{calls}, \code{paid_calls},
\code{paid_in}, \code{paid_out}, \code{usd}. \code{sum(x$usd)} is the run's cost. A model with
no registered price contributes \code{NA}, so a total that silently omitted an
unpriced model is impossible.
}
\description{
Costs a trace using each step's own model and counting only the calls that
were really issued -- a call served from a \code{\link[=gr_cache]{gr_cache()}} or a
\code{\link[=gr_replay_client]{gr_replay_client()}} spent nothing, however many tokens its prompt contained.
}
\details{
This is why the token totals on \code{\link[=gr_trace_summary]{gr_trace_summary()}} are not a bill. They
report how large the prompts and replies were, which is the right measure of
a run's \emph{shape}; a fully cached re-run has the same shape as the original and
cost nothing at all.
}
\examples{
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")
ans <- answer_document(readgpt_example(), "What was revenue?", "fast", client = cl)
gr_trace_cost(ans$trace)

# Priced by the model on the STEP -- the recipe's model -- not by the mock
# that answered. A cached re-run costs nothing for a different reason:
# paid_calls falls to zero while calls does not.
cache <- gr_cache(file.path(tempdir(), "readgpt-cost-example"))
again <- answer_document(readgpt_example(), "What was revenue?", "fast",
                         client = gr_cache_client(cl, cache))
twice <- answer_document(readgpt_example(), "What was revenue?", "fast",
                         client = gr_cache_client(cl, cache))
gr_trace_cost(twice$trace)[c("calls", "paid_calls", "usd")]
}
\seealso{
\code{\link[=gr_trace_summary]{gr_trace_summary()}}, \code{\link[=gr_estimate_cost]{gr_estimate_cost()}}, \code{\link[=gr_cache]{gr_cache()}},
\code{\link[=gr_read_many]{gr_read_many()}}
}
