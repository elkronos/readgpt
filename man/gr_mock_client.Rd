% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-client.R
\name{gr_mock_client}
\alias{gr_mock_client}
\title{A deterministic offline client for tests, demos and dry runs}
\usage{
gr_mock_client(handler = NULL, embed_handler = NULL)
}
\arguments{
\item{handler}{Function of \verb{(messages, params)} returning a string or a
\link{gr_result}.}

\item{embed_handler}{Function of \verb{(texts, params)} returning a numeric matrix
with one row per input.}
}
\value{
An object of class \code{gr_client}, with \verb{$calls()}, \verb{$embeds()} and
\verb{$reset()}.
}
\description{
\code{handler} receives \verb{(messages, params)} and returns either a string or a
\code{gr_result}. Every call is recorded in \verb{$calls()}, so tests can assert on the
exact prompts a strategy produced -- which is how you prove two reading
strategies are actually different.
}
\details{
Three things to know about the mock. It registers two model ids
(\code{"mock-model"}, \code{"mock-embed"}) in the session's model registry the first
time it is called, so they appear in \code{\link[=gr_models]{gr_models()}} afterwards. Its default
handler returns plain text, so readers that need JSON-schema output
(\code{rerank}, \code{iterative}) take their documented degraded path unless your
handler returns valid JSON for those prompts. And \code{\link[=gr_embed]{gr_embed()}} short-circuits
for mock clients: it always reports \code{embedding_source = "api"} and never
takes the lexical fallback, so that degradation cannot be exercised offline.
}
\examples{
cl <- gr_mock_client(function(messages, params) "mock answer")
gr_call(cl, list(list(role = "user", content = "hi")))$text

# `$calls()` is how you prove two reading strategies differ: it records the
# exact prompts each one sent.
cl$reset()
ch <- gr_segment(gptread_example(), list(method = "paragraph", max_tokens = 150))
invisible(gr_read(ch, "What was revenue?", cl, "skim"))
table(vapply(cl$calls(), function(x) x$label, character(1)))
}
\seealso{
\code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_call]{gr_call()}}, \link{gr_result}

\code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_call]{gr_call()}}, \link{gr_result}, \code{\link[=gptread_example]{gptread_example()}} for a
document to run against
}
