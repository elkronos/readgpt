% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{gr_reference}
\alias{gr_reference}
\title{Draw a sample to screen by hand}
\usage{
gr_reference(
  screening,
  n = 50,
  of = c("excluded", "kept", "all"),
  seed = NULL,
  path = NULL,
  blind = TRUE
)
}
\arguments{
\item{screening}{A \code{gr_screening} from \code{\link[=gr_screen]{gr_screen()}}.}

\item{n}{How many to draw. \code{Inf} takes the whole frame.}

\item{of}{Which rows to sample from: \code{"excluded"}, \code{"kept"} (include and
unclear), or \code{"all"}.}

\item{seed}{Passed to \code{\link[withr:with_seed]{withr::with_seed()}} so the draw is reproducible. A
calibration sample is part of the method and has to be re-drawable.}

\item{path}{Where to write the CSV. \code{NULL} returns the frame without writing.}

\item{blind}{Leave the model's decision and reason out of the file. On by
default: a person shown the answer agrees with it, and the resulting figure
measures nothing.}
}
\value{
A data frame of class \code{gr_reference_frame}, invisibly when written.
The \code{human_decision} column is empty and is yours to fill with \code{include} or
\code{exclude}.
}
\description{
Picks documents out of a \code{\link[=gr_screen]{gr_screen()}} run for a person to judge without
seeing what the model said, and writes them to a CSV to fill in. Feeding the
completed file back to \code{\link[=gr_calibrate]{gr_calibrate()}} is what turns "we used an LLM" into a
claim with a number attached.
}
\section{Which rows to sample}{

\code{of = "excluded"} is usually the right answer and is not the obvious one.
Sensitivity failures hide among the exclusions — a study the screener threw
away is gone, and nothing downstream will ever mention it — while the records
it kept are going to be read by a person anyway. Sampling everything at a
realistic inclusion rate spends most of the sample confirming exclusions that
were never in doubt, and leaves two or three positives to estimate
sensitivity from, which is no estimate at all.

The design that matches how people actually work is to judge \emph{every} record
the screener kept, plus a sample of what it discarded: \code{of = "kept"} with
\code{n = Inf}, and \code{of = "excluded"} with whatever hand-screening effort you have.
\code{\link[=gr_calibrate]{gr_calibrate()}} knows which frame it was given and will not compute a
corpus-wide figure from a sample that cannot support one.
}

\examples{
tab <- data.frame(document = paste0("d", 1:6, ".pdf"),
                  decision = c("include", "exclude", "exclude",
                               "unclear", "exclude", "include"),
                  reason = "because", stringsAsFactors = FALSE)
gr_reference(structure(list(table = tab), class = "gr_screening"),
             n = 3, of = "excluded", seed = 1)
}
\seealso{
\code{\link[=gr_calibrate]{gr_calibrate()}}, \code{\link[=gr_screen]{gr_screen()}}

Other corpus functions: 
\code{\link{gr_calibrate}()},
\code{\link{gr_inventory}()},
\code{\link{gr_records}()},
\code{\link{gr_search}()}
}
\concept{corpus functions}
