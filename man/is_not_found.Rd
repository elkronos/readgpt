% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-core.R
\name{is_not_found}
\alias{is_not_found}
\title{Did the model report that the document does not contain the answer?}
\usage{
is_not_found(x)
}
\arguments{
\item{x}{An answer string, or \code{ans$answer}.}
}
\value{
\code{TRUE} if the string is the not-found sentinel (or blank).
}
\description{
Every reader is instructed to reply with exactly \code{"NOT_IN_DOCUMENT"} when the
excerpts do not answer the question. Use this rather than
\code{grepl("NOT_IN_DOCUMENT", ans$answer)}: a real answer can quote the sentinel
("the log said NOT_IN_DOCUMENT, but revenue was 45.2 million"), and models do
not reproduce the token byte-exactly -- they wrap it in quotes, bold it, or
add a full stop. This matches the sentinel \emph{alone}, modulo that decoration,
and treats a blank answer as not-found too.
}
\details{
The v1 test was \code{grepl("not found|no information|not applicable", ...)} over
the whole response, which threw away every answer that happened to contain
one of those phrases.
}
\examples{
is_not_found("NOT_IN_DOCUMENT")
is_not_found("**NOT_IN_DOCUMENT.**")     # models decorate it
is_not_found("")                          # nothing came back

# A real answer that merely mentions the sentinel is NOT not-found.
is_not_found("The log said NOT_IN_DOCUMENT, but revenue was 45.2 million.")
}
\seealso{
\link{gr_answer}, \code{\link[=gr_compare]{gr_compare()}}, whose \code{summary$not_found} column is
this predicate applied per recipe

Other reading functions: 
\code{\link{gr_answer}},
\code{\link{gr_read}()},
\code{\link{gr_read_spec}()},
\code{\link{gr_reader_signature}()},
\code{\link{gr_readers}()},
\code{\link{gr_register_reader}()},
\code{\link{new_answer}()}
}
\concept{reading functions}
