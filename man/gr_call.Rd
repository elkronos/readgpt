% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-client.R
\name{gr_call}
\alias{gr_call}
\title{Call a model}
\usage{
gr_call(
  client,
  messages,
  model = NULL,
  max_output = NULL,
  temperature = NULL,
  schema = NULL,
  schema_name = "result",
  trace = NULL,
  label = "call",
  ...
)
}
\arguments{
\item{client}{A \code{gr_client}.}

\item{messages}{A list of \code{list(role=, content=)} items, or a single string,
which is wrapped as one user message. \code{role} may be \code{"system"},
\code{"developer"}, \code{"user"} or \code{"assistant"}.}

\item{model}{Overrides the client default.}

\item{max_output}{Maximum completion tokens. Clamped to the model's limit
and to what the context window actually leaves after the prompt.}

\item{temperature}{Sampling temperature; dropped automatically for reasoning
models that reject it.}

\item{schema}{Optional JSON Schema (as a list) requesting structured output.}

\item{schema_name}{Name for the schema.}

\item{trace}{Optional \code{gr_trace} to record this call into.}

\item{label}{Short label for the trace entry.}

\item{...}{Extra top-level body fields.}
}
\value{
A \link{gr_result}. \strong{Never} \code{NULL}; \verb{$text} is always a single string,
\code{""} on failure. A successful HTTP call that returned no text (a refusal, a
content filter) is reported as \code{ok = FALSE}, so an empty completion is never
passed downstream as evidence. \verb{$cached} is \code{TRUE} when the response came
from a \code{\link[=gr_cache]{gr_cache()}} or a \code{\link[=gr_replay_client]{gr_replay_client()}} instead of the network.
}
\description{
Call a model
}
\examples{
cl <- gr_mock_client(function(messages, params) "The answer is 42.")
res <- gr_call(cl, "What is the answer?")
c(ok = res$ok, text = res$text)

# A failed call still returns a usable object.
bad <- gr_call(gr_mock_client(function(m, p) stop("network down")), "hi")
c(ok = bad$ok, text = sprintf("<\%s>", bad$text), error = bad$error)
}
\seealso{
\code{\link[=gr_client]{gr_client()}}, \code{\link[=gr_mock_client]{gr_mock_client()}}, \link{gr_result}, \code{\link[=gr_budget]{gr_budget()}},
\code{\link[=gr_cache_client]{gr_cache_client()}}, \code{\link[=gr_replay_client]{gr_replay_client()}}
}
