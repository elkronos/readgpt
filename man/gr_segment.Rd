% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment.R
\name{gr_segment}
\alias{gr_segment}
\title{Segment a document into chunks}
\usage{
gr_segment(doc, spec = NULL, client = NULL, trace = NULL)
}
\arguments{
\item{doc}{A \code{gr_document} from \code{gr_ingest()}, or anything \code{gr_ingest()}
accepts.}

\item{spec}{A \code{gr_segment_spec}, a segmenter name, or a named list.}

\item{client}{A \code{gr_client}, needed only by \code{semantic}, \code{proposition} and
\code{contextual(context_source = "llm")}.}

\item{trace}{Optional \code{gr_trace}. When none is given one is made, so the
model calls a segmenter makes are held to \code{gr_options(max_calls =, max_cost_usd =)} and recorded, and it is returned as \verb{$trace}.}
}
\value{
A \link{gr_chunks} object. If the requested segmenter could not run it
falls back and records the fallback in \verb{$method}, e.g.
\code{"semantic->paragraph"} when no \code{client} was supplied. \code{proposition} and
\code{contextual} chunks carry a \code{source_text} column: the document text each
chunk was made from, without the rewrite or the context line, which is
what a quote from the chunk is checked against. When no \code{trace} was
passed, \verb{$trace} is the one this call recorded into.
}
\description{
Segmentation is deliberately separate from ingestion: one extraction can feed
many different segmentations, so comparing chunking strategies costs one file
read, not one per strategy.
}
\examples{
doc <- gr_ingest(readgpt_example())

# The same document, three boundary hypotheses. No API calls.
do.call(rbind, lapply(c("fixed", "paragraph", "sentence", "structural"),
  function(m) gr_chunk_stats(gr_segment(doc, list(method = m, max_tokens = 120)))))
}
\seealso{
\code{\link[=gr_segmenters]{gr_segmenters()}}, \code{\link[=gr_segment_spec]{gr_segment_spec()}}, \code{\link[=gr_chunk_stats]{gr_chunk_stats()}}, \link{gr_chunks}

Other segmentation functions: 
\code{\link{gr_chunk_stats}()},
\code{\link{gr_chunks}},
\code{\link{gr_register_segmenter}()},
\code{\link{gr_segment_spec}()},
\code{\link{gr_segmenters}()},
\code{\link{new_chunks}()}
}
\concept{segmentation functions}
