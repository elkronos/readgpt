% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-cache.R
\name{gr_cache_stats}
\alias{gr_cache_stats}
\title{Cache statistics}
\usage{
gr_cache_stats(cache)
}
\arguments{
\item{cache}{A \code{\link[=gr_cache]{gr_cache()}}.}
}
\value{
A one-row data frame: \code{dir}, \code{entries}, \code{bytes}, \code{hits}, \code{misses},
\code{writes}. \code{entries} and \code{bytes} are read from disk, so they include entries
written by earlier sessions; the three counters cover this session only.
}
\description{
Cache statistics
}
\examples{
cache <- gr_cache(file.path(tempdir(), "readgpt-stats-cache"))
cl <- gr_cache_client(gr_mock_client(function(m, p) "hi"), cache)
invisible(gr_call(cl, "one"))
invisible(gr_call(cl, "one"))
gr_cache_stats(cache)[c("entries", "hits", "misses", "writes")]
}
\seealso{
\code{\link[=gr_cache]{gr_cache()}}, \code{\link[=gr_cache_clear]{gr_cache_clear()}}
}
