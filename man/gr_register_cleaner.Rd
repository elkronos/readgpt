% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-clean.R
\name{gr_register_cleaner}
\alias{gr_register_cleaner}
\title{Register a cleaning step}
\usage{
gr_register_cleaner(
  name,
  fn,
  stage = c("early", "late"),
  description = "",
  default_on = FALSE,
  scope = c("block", "document")
)
}
\arguments{
\item{name}{Step name.}

\item{fn}{Function of \verb{(text, opts)} returning cleaned text.}

\item{stage}{\code{"early"} (structure-preserving, e.g. boilerplate removal) or
\code{"late"} (destructive normalisation, e.g. digit stripping). Early steps
always run before late steps regardless of the order the user lists them.}

\item{description}{One-line description.}

\item{default_on}{Whether the step is enabled by the standard preset.}

\item{scope}{\code{"block"} (default) applies the step to each text block
independently. \code{"document"} applies it once to all blocks joined together,
which is required for anything that reasons about position or repetition
across the whole document (dropping a trailing bibliography, or detecting
a running head by how often a line recurs). A document-scoped step that was
applied per block would simply never fire. Its result is matched back to
the blocks line by line, so such a step should remove whole lines, or cut
the text at the start of a line, and leave the lines it keeps as they
were: each block then keeps whichever of its lines survived. A step that
rewrites text inside lines is matched line for line when it keeps every
line; otherwise it is applied to each block on its own, with a
\code{gr_clean_unmapped} warning.}
}
\value{
Invisibly, \code{name}.
}
\description{
Register a cleaning step
}
\examples{
gr_register_cleaner("drop_confidential", stage = "early",
  description = "Remove CONFIDENTIAL banner lines",
  fn = function(x, o) gsub("(?mi)^\\\\s*CONFIDENTIAL.*$", "", x, perl = TRUE))

gr_clean("CONFIDENTIAL - DRAFT\n\nThe real content of the document.",
         steps = c("drop_confidential", "collapse_whitespace"))
}
\seealso{
\code{\link[=gr_cleaners]{gr_cleaners()}}, \code{\link[=gr_clean]{gr_clean()}}, \code{\link[=gr_ingest_spec]{gr_ingest_spec()}}

Other ingest functions: 
\code{\link{gr_clean}()},
\code{\link{gr_cleaners}()},
\code{\link{gr_document}},
\code{\link{gr_extractors}()},
\code{\link{gr_ingest}()},
\code{\link{gr_ingest_spec}()},
\code{\link{gr_register_extractor}()}
}
\concept{ingest functions}
