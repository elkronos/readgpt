% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protocol.R
\name{gr_protocols}
\alias{gr_protocols}
\title{Protocols that ship with the package, and any you have registered}
\usage{
gr_protocols(name = NULL)
}
\arguments{
\item{name}{A protocol name, or omit to list them all.}
}
\value{
With \code{name}, a \code{\link[=gr_protocol]{gr_protocol()}}. Without, a data frame of \code{name},
\code{question} and \code{description}.
}
\description{
Three starting points. They are \strong{templates, not standards}: the package
knows the shape a protocol has, not what your criteria should be, and every
field of a built-in is meant to be edited before it is used.
}
\details{
\describe{
\item{\code{bibliography}}{Who wrote it, what it is called, where it appeared.
No screening -- everything is included -- so it is the cheapest way to
turn a folder into a reference list you can check.}
\item{\code{evidence_table}}{One row per study: design, population, comparison,
outcome, effect. No synthesis outline; the table is the output.}
\item{\code{systematic_review}}{Criteria, a schema and a write-up outline, in
the shape a report following a standard like PRISMA expects. Filling it in
is your work, not the package's.}
}
}
\examples{
gr_protocols()
gr_protocols("bibliography")$fields
}
\seealso{
\code{\link[=gr_protocol]{gr_protocol()}}, \code{\link[=gr_register_protocol]{gr_register_protocol()}}, \code{\link[=gr_extract]{gr_extract()}}
}
