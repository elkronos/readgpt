% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-replay.R
\name{gr_replay_client}
\alias{gr_replay_client}
\title{A client that answers from a recorded run}
\usage{
gr_replay_client(source, strict = TRUE)
}
\arguments{
\item{source}{A \code{gr_trace}, a path to a file written by \code{\link[=gr_trace_save]{gr_trace_save()}}, or
an already-parsed list in that shape.}

\item{strict}{If \code{TRUE} (default), a prompt with no recorded response raises
a \code{gr_replay_miss} error. That is usually what you want: a miss means the
replay has diverged from the recording, and continuing would produce a
result that looks like the original but is not. With \code{FALSE} a miss returns
a failed \link{gr_result} instead, so a partially recorded trace still runs.}
}
\value{
An object of class \code{gr_replay_client}, usable anywhere a
\code{\link[=gr_client]{gr_client()}} is. It also carries \verb{$stats()} and \verb{$missed()}.
}
\description{
Replays the responses in a trace instead of calling a model. Give it the
trace from a run and the same document and question, and you get that run
back -- same answers, same evidence, same merge decisions -- with no API key,
no network and no spend.
}
\details{
This is what makes a published result checkable. Ship the trace next to the
paper and a reader can reproduce the run rather than take it on trust. It is
also the cheapest possible bug report: a trace file is a re-runnable
recording of exactly what went wrong.
}
\section{Matching}{

A response is matched on the exact prompt messages plus the model id. When a
run issued the same prompt more than once -- which happens at a temperature
above zero, and in readers that revisit a chunk -- the recorded responses are
returned in the order they were produced. Once they are exhausted the last
one repeats.
}

\section{Embeddings}{

Embeddings are not model calls and are not recorded in a trace, so whether a
replay reproduces a run's chunk \emph{ranking} depends on how the run embedded --
and that is checked rather than assumed. The ranking reproduces exactly when
the recording used a \strong{deterministic} embedder and the replay uses the
\strong{same} one; both conditions, because replaying an API-embedded run with a
deterministic local embedder would compute vectors the original never saw
while looking exact. Anything else falls back to hashed lexical vectors and
warns with class \code{gr_replay_no_embeddings}; every recorded answer is still
reproduced, but the ranking may differ. Record a run you intend to publish
with \code{gr_options(embedder = "lexical")}, or with your own embedder registered
as \code{deterministic = TRUE}.
}

\section{What does not replay}{

Traces do not record the JSON schema a call requested, so two calls that
differ only by schema share a recording.
}

\examples{
# A run.
cl <- gr_mock_client(function(m, p) "Revenue was 45.2 million dollars.")
ans <- answer_document(readgpt_example(), "What was revenue?", "fast", client = cl)

# The same run, from the recording, with no client and no key.
rp <- gr_replay_client(ans$trace)
again <- answer_document(readgpt_example(), "What was revenue?", "fast", client = rp)
identical(again$answer, ans$answer)

rp$stats()

# Through a file, which is how a run reaches someone else.
f <- tempfile(fileext = ".json")
gr_trace_save(ans$trace, f)
answer_document(readgpt_example(), "What was revenue?", "fast",
                client = gr_replay_client(f))$answer
}
\seealso{
\code{\link[=gr_trace_save]{gr_trace_save()}}, \code{\link[=gr_cache]{gr_cache()}} for making future runs cheap,
\code{\link[=gr_mock_client]{gr_mock_client()}} for invented answers rather than recorded ones
}
