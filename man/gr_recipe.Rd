% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{gr_recipe}
\alias{gr_recipe}
\title{Bind an ingestion, segmentation and reading configuration together}
\usage{
gr_recipe(name = NULL, ingest = NULL, segment = NULL, read = NULL)
}
\arguments{
\item{name}{Label used in results and traces.}

\item{ingest}{A \code{gr_ingest_spec}, preset name, or named list.}

\item{segment}{A \code{gr_segment_spec}, segmenter name, or named list.}

\item{read}{A \code{gr_read_spec}, reader name, or named list.}
}
\value{
A \code{gr_recipe}: a list of \code{name}, \code{ingest}, \code{segment} and \code{read}, each
a validated spec. It has a \code{print()} method that shows all three at once.
}
\description{
A recipe is the unit \code{\link[=gr_compare]{gr_compare()}} treats as one experiment. Fixing two axes
and varying the third is what makes a difference in the answer attributable
to the change you made -- which is exactly what the previous release could not
do, because its modes shared one chunk object.
}
\examples{
gr_recipe("semantic_topk", segment = list(method = "semantic", max_tokens = 800),
          read = list(reader = "retrieve", top_k = 6))

# Start from a built-in and change one axis -- the other two stay fixed, so
# any difference in the answer is attributable to the change.
r <- gr_recipes("thorough")
r$segment <- gr_segment_spec(method = "structural", max_tokens = 1200)
r$name <- "thorough_structural"
r
}
\seealso{
\code{\link[=gr_recipes]{gr_recipes()}} for the built-ins, \code{\link[=answer_document]{answer_document()}} to run one,
\code{\link[=gr_compare]{gr_compare()}} to run several, \code{\link[=gr_ingest_spec]{gr_ingest_spec()}}, \code{\link[=gr_segment_spec]{gr_segment_spec()}},
\code{\link[=gr_read_spec]{gr_read_spec()}}
}
