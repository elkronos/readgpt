% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{gr_screen}
\alias{gr_screen}
\title{Decide which documents a review should read}
\usage{
gr_screen(
  sources,
  protocol = NULL,
  question = NULL,
  include = NULL,
  exclude = NULL,
  recipe = "research",
  client = NULL,
  store = NULL,
  screen_tokens = NULL,
  on_error = c("continue", "stop"),
  max_total_usd = NULL,
  keep_answers = FALSE,
  recursive = FALSE,
  ...
)
}
\arguments{
\item{sources}{As \code{\link[=gr_read_many]{gr_read_many()}}: file paths, a directory, or raw text.}

\item{protocol}{A \code{\link[=gr_protocol]{gr_protocol()}} carrying the criteria and the review
question. Give this, or \code{include}/\code{exclude} directly.}

\item{question, include, exclude}{The review question and the criteria, if you
are not passing a protocol. Each criterion is one statement a document
either meets or does not.}

\item{recipe, client, store, on_error, max_total_usd, recursive, keep_answers}{As
\code{\link[=gr_extract]{gr_extract()}}.}

\item{screen_tokens}{Cap what the model is shown, in tokens, counted from the
start of the document. Leaving it unset shows as much as the model's context
allows. Setting it to a few hundred is title-and-abstract screening, done
deliberately: cheaper, and closer to what a human screener sees at this
stage. Either way the \code{truncated} and \code{seen_tokens} columns say what was
actually read.}

\item{...}{Recipe overrides, as in \code{\link[=gr_extract]{gr_extract()}}.}
}
\value{
An object of class \code{gr_screening}:
\describe{
\item{\code{table}}{One row per document: \code{document}, \code{document_id},
\code{decision}, \code{reason}, \code{criterion}, \code{quote}, \code{verified}, \code{seen_tokens},
\code{document_tokens}, \code{truncated}, \code{status}, \code{duplicate_of}, \code{error}.}
\item{\code{included}}{The distinct sources whose decision was \code{"include"} --
the paths, not the display labels, so this is the argument to hand
straight to \code{\link[=gr_extract]{gr_extract()}}. Duplicates are left out; they are the same
study, and \code{table} still has their rows.}
\item{\code{summary},\code{answers},\code{trace},\code{store}}{As \code{\link[=gr_extract]{gr_extract()}}.}
}
}
\description{
The stage before \code{\link[=gr_extract]{gr_extract()}}. One model call per document, a decision and a
reason for every one, and nothing dropped on the way.
}
\section{Three decisions, not two}{

\code{decision} is \code{"include"}, \code{"exclude"} or \code{"unclear"}. The third is not a
failure mode -- it is the answer when the excerpt does not settle the
question, and it is what stops an uncertain call from being recorded as a
confident one. Those documents are for a person to look at. Every published
evaluation of automated screening reaches the same conclusion: reliable on the
easy calls, not yet trustworthy alone on the hard ones.

A document that could not be read at all gets \code{status = "failed"} and no
decision. It is not excluded, and it is not silently absent: it is an
outstanding job. A review whose denominator is unknown is not a review.
}

\section{Reporting it}{

\code{table(x$table$decision)} is the screening result and
\code{table(x$table$criterion)} is the breakdown by exclusion criterion, which is
what a flow diagram asks for. Duplicates were removed before screening, so
\code{sum(!is.na(x$table$duplicate_of))} is the "duplicates removed" count and
every one of them still has a row -- see \code{\link[=gr_read_many]{gr_read_many()}}.
}

\examples{
cl <- gr_mock_client(function(messages, params) {
  '{"decision":"include","reason":"Reports a randomised comparison.",
    "criterion":"Reports a randomised comparison",
    "quote":"We randomly assigned participants to two groups."}'
})

f <- tempfile(fileext = ".txt")
writeLines("We randomly assigned participants to two groups.", f)

s <- gr_screen(f, question = "Does the treatment work?",
               include = "Reports a randomised comparison", client = cl)
s$table[, c("document", "decision", "reason")]
}
\seealso{
\code{\link[=gr_protocol]{gr_protocol()}}, \code{\link[=gr_extract]{gr_extract()}}, \code{\link[=gr_read_many]{gr_read_many()}}
}
