% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-models.R
\name{gr_budget}
\alias{gr_budget}
\title{Compute a usable input-token budget for one model call}
\usage{
gr_budget(
  model = NULL,
  reserve_output = NULL,
  overhead = 0,
  safety_margin = NULL
)
}
\arguments{
\item{model}{Model id.}

\item{reserve_output}{Tokens to reserve for the completion. An explicit value
is clamped to the model's \code{max_output}. \code{NULL} reserves
\code{min(max_output, max(min_output_tokens, usable / 4))}, where \code{usable} is the
context window after \code{safety_margin} and \code{min_output_tokens} is a
\code{\link[=gr_options]{gr_options()}} setting (default 256).}

\item{overhead}{Tokens consumed by system prompts, question text and message
framing that are not part of the document payload.}

\item{safety_margin}{Fraction of the context window left unused to absorb
tokenizer error. Defaults to the \code{safety_margin} option; clamped to
[0, 0.5].}
}
\value{
A list with \code{input}, \code{output}, \code{context_window}, \code{overhead}, \code{margin}
and \code{certain} (\code{FALSE} when the model's limits were guessed rather than
known -- see \code{\link[=gr_model_info]{gr_model_info()}}).
}
\description{
This is the single arithmetic chokepoint that every prompt-building path in
the package must go through. It has three guarantees:
}
\details{
\itemize{
\item the returned \code{input} budget is always \strong{strictly positive};
\item \code{input + output <= context_window * (1 - safety_margin)};
\item if no positive budget exists, it raises a \code{gr_budget_error} naming the
parameter to change, rather than returning a negative number that silently
corrupts every downstream \code{split()} and \code{while} loop.
}
}
\examples{
unlist(gr_budget("gpt-4o", reserve_output = 1024, overhead = 200))

# The same question leaves very different room on different models.
vapply(c("gpt-4", "gpt-4o", "gpt-5.6-terra"),
       function(m) gr_budget(m)$input, numeric(1))

# No positive budget is an error naming what to change, never a negative
# number that silently reverses the document downstream.
tryCatch(gr_budget("gpt-4", overhead = 9000),
         gr_budget_error = function(e) conditionMessage(e))
}
\seealso{
\code{\link[=gr_model_info]{gr_model_info()}}, \code{\link[=gr_estimate_cost]{gr_estimate_cost()}}, \code{\link[=gr_count_tokens]{gr_count_tokens()}}

Other cost and token functions: 
\code{\link{gr_count_tokens}()},
\code{\link{gr_estimate_cost}()},
\code{\link{gr_model_info}()},
\code{\link{gr_model_limits}()},
\code{\link{gr_models}()},
\code{\link{gr_register_model}()},
\code{\link{gr_set_tokenizer}()},
\code{\link{gr_tokenizer}()},
\code{\link{gr_truncate_tokens}()}
}
\concept{cost and token functions}
