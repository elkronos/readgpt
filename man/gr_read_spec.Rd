% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{gr_read_spec}
\alias{gr_read_spec}
\title{Describe a reading configuration}
\usage{
gr_read_spec(
  reader = "map_reduce",
  model = NULL,
  temperature = NULL,
  max_answer_tokens = 1500L,
  max_chunk_tokens = 700L,
  max_summary_tokens = 500L,
  top_k = 6L,
  min_score = -Inf,
  mmr = 1,
  context_order = c("relevance", "document", "edges"),
  rerank_candidates = 20L,
  rerank_min_score = 4,
  fan_in = 5L,
  max_levels = 5L,
  max_rounds = 4L,
  members = NULL,
  cite = FALSE,
  skim_model = NULL,
  summary_model = NULL,
  parallel = NULL,
  delay_between_calls = 0,
  on_overflow = c("warn", "error"),
  ...
)
}
\arguments{
\item{reader}{Reader name; see \code{gr_readers()}.}

\item{model}{Chat model id.}

\item{temperature}{Sampling temperature, or \code{NULL} to omit the field. You do
not need to null it yourself for reasoning models: it is dropped
automatically for any model whose registry entry has
\code{supports_temperature = FALSE}, which includes the default model. Check with
\code{gr_model_info(model)$supports_temperature}.}

\item{max_answer_tokens}{Output cap for final answers and merges.}

\item{max_chunk_tokens}{Output cap for per-chunk calls.}

\item{max_summary_tokens}{Output cap for summarisation calls.}

\item{top_k}{For \code{retrieve}, \code{rerank} and \code{iterative}: chunks to use.}

\item{min_score}{For \code{retrieve}: chunks scoring below this are dropped, but
if that would leave nothing the single best chunk is used anyway. \code{-Inf}
disables the filter. Not a cosine similarity when embeddings fall back to
lexical vectors -- the score is then a blend of cosine and BM25.}

\item{mmr}{Diversity of selection, for \code{retrieve} and \code{iterative}. \code{1} (the
default) is plain top-k. Below 1, chunks are picked greedily by
\verb{mmr * relevance - (1 - mmr) * similarity to what is already picked}, so
three chunks saying the same thing do not all get in and pay for each other.
Costs nothing -- the vectors are already computed. \code{0.7} is a reasonable
place to start; \code{0} selects for novelty alone and will happily pick
irrelevant chunks because they are different.}

\item{context_order}{Where the selected chunks sit in the prompt.
\code{"relevance"} (default) is most relevant first; \code{"document"} restores the
order they appear in the document, which reads better when chunks are
consecutive; \code{"edges"} puts the strongest first and second-strongest last,
burying the weakest in the middle, because transformers attend measurably
better to the beginning and end of a long context than to its middle.
Selection is unaffected -- this decides only placement, and it applies to
\code{retrieve} and \code{rerank}, the two readers that put several ranked chunks in
one prompt.}

\item{rerank_candidates, rerank_min_score}{For \code{rerank}: how many chunks to
score, and the score below which a chunk is discarded.}

\item{fan_in, max_levels}{For \code{hierarchical}: summaries combined per call, and
the recursion depth cap.}

\item{max_rounds}{For \code{iterative}: retrieve-assess cycles.}

\item{members}{For \code{ensemble}: the reader names to combine.}

\item{cite}{Ask for chunk-level citations (\verb{[chunk 3]}) in the answer. Map
those ids back to pages via \code{ans$evidence}. Forced off for \code{hierarchical},
which answers from summaries: summaries carry no \verb{[chunk N]} ids, so asking
for citations there asks the model to invent them.}

\item{skim_model, summary_model}{Optional cheaper models for the per-chunk
stages. \code{skim_model} is used by \code{skim}'s extraction \strong{and} \code{rerank}'s
relevance scoring; \code{summary_model} by \code{hierarchical}'s summarisation.}

\item{parallel}{Run per-chunk calls in parallel.}

\item{delay_between_calls}{Seconds to sleep between sequential calls, for
rate-limit shaping. Honoured by \code{map_reduce}, \code{refine} and \code{skim}; the
other readers do not sleep.}

\item{on_overflow}{For \code{stuff}: \code{"warn"} (truncate and say so) or \code{"error"}.}

\item{...}{Extra fields for custom readers.}
}
\value{
A list of class \code{gr_read_spec}.
}
\description{
Describe a reading configuration
}
\section{Out-of-range values}{

Numeric arguments are clamped into a usable range and the change is warned
about, never applied silently: \code{top_k} [1, 1e4], \code{fan_in} [2, 32],
\code{max_levels} [1, 12], \code{max_rounds} [1, 20], \code{rerank_candidates} [1, 1e4],
\code{rerank_min_score} [0, 10], \code{delay_between_calls} [0, 600],
token caps [16, 1e6].
}

\examples{
gr_read_spec("retrieve", top_k = 8)$top_k

# Out-of-range settings are corrected loudly, not quietly.
suppressWarnings(gr_read_spec("hierarchical", fan_in = 999)$fan_in)
}
\seealso{
\code{\link[=gr_readers]{gr_readers()}} for the available readers and their call costs,
\code{\link[=gr_read]{gr_read()}}, \code{\link[=gr_recipe]{gr_recipe()}}

Other reading functions: 
\code{\link{gr_answer}},
\code{\link{gr_read}()},
\code{\link{gr_reader_signature}()},
\code{\link{gr_readers}()},
\code{\link{gr_register_reader}()},
\code{\link{is_not_found}()},
\code{\link{new_answer}()}
}
\concept{reading functions}
